/**
 * /atlas hides the right pane at the shell frame. Keep this slot empty so
 * route prefetches do not instantiate the Ask Civica panel.
 */
export default function AtlasMapRight() {
  return null;
}
