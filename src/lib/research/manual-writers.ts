import { indicatorHistory, partyPositions } from "@/lib/db/schema";
import { markSourcesSynced } from "@/lib/db/source-freshness";
type Db=typeof import("@/lib/db").db;
export type IndicatorHistoryInput=typeof indicatorHistory.$inferInsert; export type PartyPositionInput=typeof partyPositions.$inferInsert;
async function validateAndWrite<T extends Record<string,unknown>>(db:Db,table:typeof indicatorHistory|typeof partyPositions,rows:T[],keyOf:(row:T)=>string,sourceId:string,options:{dryRun?:boolean;markSynced?:typeof markSourcesSynced}={}){if(rows.length===0)throw new Error(`${sourceId}: zero rows`);const keys=new Set<string>();for(const row of rows){const key=keyOf(row);if(keys.has(key))throw new Error(`Duplicate ${sourceId} row: ${key}`);keys.add(key);}if(!options.dryRun){for(const row of rows){if(table===indicatorHistory)await db.insert(indicatorHistory).values(row as unknown as IndicatorHistoryInput).onConflictDoUpdate({target:[indicatorHistory.jurisdictionId,indicatorHistory.indicator,indicatorHistory.year],set:row});else await db.insert(partyPositions).values(row as unknown as PartyPositionInput).onConflictDoUpdate({target:partyPositions.legislaturePartyId,set:row});}}await(options.markSynced??markSourcesSynced)(sourceId,{rowsWritten:rows.length,dryRun:options.dryRun});return{proposed:rows.length,written:options.dryRun?0:rows.length};}
export const writeIndicatorHistory=(db:Db,rows:IndicatorHistoryInput[],options?:{dryRun?:boolean;markSynced?:typeof markSourcesSynced})=>validateAndWrite(db,indicatorHistory,rows as Record<string,unknown>[],r=>`${r.jurisdictionId}:${r.indicator}:${r.year}`,String(rows[0]?.sourceId??"indicator_history"),options);
export const writePartyPositions=(db:Db,rows:PartyPositionInput[],options?:{dryRun?:boolean;markSynced?:typeof markSourcesSynced})=>validateAndWrite(db,partyPositions,rows as Record<string,unknown>[],r=>String(r.legislaturePartyId),String(rows[0]?.sourceId??"vparty"),options);
