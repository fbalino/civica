# Civica Academic Publication Readiness — Progress

Append one plain-language line for every completed checklist task. Each line names the task, outcome, commit, and evidence path. Do not log activity that did not satisfy the task's `Done when`.

## 2026-07-09

- Planning baseline established: product direction, experimental-product posture, orchestration policy, Blind Audit protocol, and master-plan operating contract were recorded. This is plan setup, not a checked implementation task.
- Repository-grounded master plan completed: 288 atomic tasks across ten areas validate exactly against `MASTER-CHECKLIST.md`; the blind audit, Fable Index design-space study, owner overview, decisions, manual checks, evidence rules, and orchestration state are preserved. Implementation progress remains 0/288 by design.
- CLM-001 completed: 24 headline claims across all 14 required public surfaces now have owners, evidence, versions, gates, exact-copy markers, and a passing zero-orphan validator in commit `feat(claims): establish public claim registry and tier policy (CLM-001, CLM-002)`; evidence: `plan/evidence/CLM-001/`.
- CLM-002 completed: seven canonical evidence tiers now define allowed language and disclosure, and automated fixtures prove every registered claim maps to exactly one tier in the same commit; evidence: `plan/evidence/CLM-002/`.
- CLM-003 completed: the visible product, metadata, documentation, citation abstract, footer/social copy, and internal reviewer brief now consistently make the provenance-first country atlas primary and the Index/Pulse secondary experiments in commit `feat(editorial): establish atlas-first truthful positioning (CLM-003, CLM-004)`; evidence: `plan/evidence/CLM-003/`.
- CLM-004 completed: unsupported academic-authority, validation, confidence-interval, continuous-daily-measure, advisory-endorsement, blanket reuse, and coverage claims are removed or explicitly qualified, with repository/rendered guards and current Pulse runtime evidence in the same commit; evidence: `plan/evidence/CLM-004/`.
- CLM-005 completed: the Index remains active while country letter grades, qualitative score bands, score-driven traffic-light colors, migration theater, and public band fields are replaced by a neutral numeric position and input-variation range across UI, APIs, embeds, exports, metadata, and prose in commit `feat(index): remove country grades without retiring the Index (CLM-005)`; evidence: `plan/evidence/CLM-005/`.
- CLM-006 completed: current public coverage/count claims now resolve from registered runtime state with nonnumeric fallbacks or identify a frozen release/date, and a TypeScript/markdown audit rejects stale literals across the canonical public source set in commit `feat(claims): make public coverage counts self-verifying (CLM-006)`; evidence: `plan/evidence/CLM-006/`.

## 2026-07-10

- CLM-007 completed: Pulse now publishes a generated and live-checked `pulse-v2.1-beta` runtime contract; public pages/APIs/docs agree on observed feeds, providers by role, daily cadence, review gates, mixed legacy coverage, experimental dimensional effects, and the absence of a scalar Pulse score, with hardened parser/review/scoring invariants and final browser/Sonnet review in commit `feat(pulse): publish self-verifying runtime contract (CLM-007)`; evidence: `plan/evidence/CLM-007/`.
- CLM-008 completed: the active Index remains a secondary research-Beta composite, but its public methodology, Beta rankings dimensions, comments, metadata, and executable worked examples now agree on the four real sources, fixed-bound transforms, partial re-proportioning, rounded Monte Carlo median, input-variation range, neutral presentation, and release status in commit `feat(index): reconcile executable beta methodology (CLM-008)`; evidence: `plan/evidence/CLM-008/`.
- CLM-009 completed: a typed documentation-source registry now assigns one canonical path/symbol to methodology and release concepts across all six required surface kinds; deterministic generators, formula/route/anchor/link validation, negative fixtures, and production browser checks keep mirrors aligned and invisible to readers in commit `feat(docs): enforce canonical documentation sources (CLM-009)`; evidence: `plan/evidence/CLM-009/`.
- CLM-010 completed: the replication route is now a fail-closed `Not published` status surface with a typed nine-component ledger, explicit G2/G3 ownership, zero artifact links, a registered non-availability claim, negative fixtures, and a build validator that prevents premature availability or publication in commit `feat(replication): publish honest package status (CLM-010)`; evidence: `plan/evidence/CLM-010/`.
- CLM-011 completed: operational documentation and project memory now match direct routes, real npm commands, the 49-table schema, broad cron scope, current Atlas redirects, generated README sources, and present-tense reconciliation rules, with a DB-free build validator and production browser proof in commit `docs: enforce operational documentation truth (CLM-011)`; evidence: `plan/evidence/CLM-011/`.
- CLM-012 completed: all 14 live versioned GET routes and the country bulk export now share strict runtime response schemas, generated documentation/examples, contract tests, canonical branch-aware deprecation metadata and headers, public numeric-claim coverage, live-database verification, and production browser proof in commit `feat(api): enforce generated response contracts (CLM-012)`; evidence: `plan/evidence/CLM-012/`.
- CLM-013 completed: one apex metadata policy, stable source-backed sitemap dates, strict Dataset JSON-LD, research-posture metadata, 53 adversarial fixtures, a DB-free build guard, and a production crawler now enforce canonical/OG/social/structured-data truth across all 843 sitemap URLs in commit `feat(metadata): enforce apex crawler contract (CLM-013)`; evidence: `plan/evidence/CLM-013/`.
- CLM-014 completed: country/territory captions, About, and a canonical Licensing policy now disclose AI-assisted non-documentary editorial illustrations, incomplete launch-corpus records and QA, correction handling, and conservative reuse rights; a fail-closed build guard, 38 adversarial fixtures, Fable placement correction, production browser proof, and Opus acceptance prevent silent overclaim or renewed tile obstruction in commit `feat(art): publish honest illustration policy (CLM-014)`; evidence: `plan/evidence/CLM-014/`.
- CLM-015 completed: one typed contract now defines 14 research terms and generates their public glossary entries; a deterministic build guard checks eight registered methodology/replication surfaces and rejects unsupported Civica validation, peer-review, confidence-interval, and independent-replication claims without banning ordinary scholarship, with 15 focused fixtures, production browser proof, and Sonnet 5 acceptance in commit `feat(research): enforce canonical terminology (CLM-015)`; evidence: `plan/evidence/CLM-015/`.
- CLM-016 completed: a canonical `/policies` document now governs correction, retraction, supersession, versioning, known limitations, API/data handling, preservation, and notification across a closed six-artifact registry; 21 pure fixtures generate exact correction/retraction/clarification records and fail on overpromises or missing current-capability boundaries, with production browser proof and Opus 4.8 acceptance in commit `feat(policy): publish executable correction contract (CLM-016)`; evidence: `plan/evidence/CLM-016/`.
- CLM-017 completed: `npm run validate:claims-docs` now composes eleven existing validators into one fail-closed, DB-free gate spanning all seven required claims/documentation categories; per-category orchestration fixtures, semantic negative tests, a read-only push/PR workflow, 323 passing tests, and a production build prove stale or missing checks cannot silently pass in commit `ci(claims): enforce unified documentation gate (CLM-017)`; evidence: `plan/evidence/CLM-017/`.
- CLM-018 completed: one explicitly interim artifact-class rights registry now separates free access from reuse permission across footer, About, Licensing, Terms, metadata, downloads/API docs, embeds, citation UI, `CITATION.cff`, README, and launch prose; it states mixed upstream rights, no complete DAT-003 manifest, no current code-license grant, citation-not-permission, and hosted-embed limits, while a twelfth aggregate gate child, 23 adversarial fixtures, a 308-file public-source sweep, 349 passing tests, browser proof, a production build, and Opus 4.8 acceptance enforce the posture; evidence: `plan/evidence/CLM-018/`.
- CLM-019 completed: universal per-value provenance promises are replaced by a generated compact-renderer audit covering ten classes across home, Atlas, rankings, and embeds; four classes currently expose source, vintage, and rights at point of use, six named exceptions remain public, dataset-wide fact coverage stays assigned to DAT-005, and a thirteenth aggregate gate child, seven adversarial fixtures, a 320-file public-source sweep, 356 passing tests, browser proof, and a production build enforce the measured 4/10 (40%) claim in commit `fix(provenance): publish measured compact coverage (CLM-019)`; evidence: `plan/evidence/CLM-019/`.
- DAT-001 completed: every scheduled producer and ten manual Atlas/Index/Conditions adapter families now resolve to release-owned code; the four-source 2024-Q4 Index can be reconstructed from publisher files with exact input hashes, exact row counts, and identical six-decimal semantic checksums across five source/dimension groups, while the 20-row WGI Voice & Accountability coverage substitution is explicit rather than hidden; canonical pure parsers, frozen release coverage, source-derived CPI/HDI metric paths, fixture tests, browser proof, a production build, and clean-room verification replace dependence on an unmerged branch; evidence: `plan/evidence/DAT-001/`.
- DAT-002 completed: one typed `source-input-manifest/v1` contract now closes all 45 deployed ingestion/reconciliation pipelines and 43 external source specifications; the checked `ci-beta-2024-Q4` manifest records exact access URLs, retrieval time, publisher bytes hashes, upstream versions/vintages, formats, coverage, redistribution posture, and a content-derived adapter version for all four released Index inputs, while generated release attempts fail on any absent capture and the not-yet-released G2 Atlas honestly reports 57 still-required pipeline/source captures rather than inventing retrospective hashes; evidence: `plan/evidence/DAT-002/`.
- DAT-003 completed: one typed `rights-manifest/v1` contract now covers all 43 production sources, public field classes, two export products, and the frozen Index release artifact; six source terms are verified, 37 unresolved or restricted terms fail closed, the release artifact names included metadata and excluded publisher payloads, and the unsafe legacy country JSON/CSV export returns an explicit 503 until DAT-017/DAT-027 replaces it with a rights-filtered product; 375 tests, the full claims/docs gate, a production build, and desktop/mobile browser checks passed; evidence: `plan/evidence/DAT-003/`.
- DAT-004 completed: `jurisdiction-status/v1` replaces the live database's blanket 253-row `sovereign_state` default with a closed, sourced five-class taxonomy and display policy: 194 sovereign states, 47 dependencies/territories, two associated states, eight disputed/limited-recognition entries, and two aggregate/special areas; source arrays, review date, neutral status note, administering relationship, and dispute flag now persist per row, unknown entries fail closed, the targeted forward migration applied transactionally, the live audit passed all 253 rows, the public country API now reports the correct 194-row sovereign scope, eight edge fixtures, browser checks, 383 tests, and the production build passed; evidence: `plan/evidence/DAT-004/`.
- DAT-005 completed: a checked `fact-provenance-coverage/v1` snapshot and public reader/JSON surfaces now measure 17,516 active country/fact-key groups from 25,824 source observations, 17,516 fully source-linked groups, 13,201 single-source groups, 2,349 groups passing the conservative two-plus-native-publisher screen, zero unresolved disputes, zero stale live rows under the declared 180-day rule, and 7,875/7,875 linked statement rows; complete 253-country/area and 88-fact-key breakdowns, a DB generator, DB-free consistency gate, five adversarial fixtures, numeric/public-claim registration, browser checks, 388 tests, and the production build prevent dataset coverage from being confused with the separate 4/10 compact-renderer audit; evidence: `plan/evidence/DAT-005/`.
- DAT-006 completed: all 129 canonical fact keys now carry one explicit policy—37 single-source passthrough, 27 multi-source resolver, 24 manual review, and 41 unsupported—and a checked claim-level lineage audit maps all 172 active source/fact relationships; republishers collapse into their producing family, projections do not corroborate measurements, and compilations or unknown lineage fail closed, reducing the current two-plus-independent fact count from the provisional source-ID screen to 458; ten independence/edge fixtures, all eight live resolver examples, 398 tests, the full claims gate, production build, machine-readable API, and desktop/mobile browser checks passed in commit `feat(reconciliation): enforce source-family audit (DAT-006)`; evidence: `plan/evidence/DAT-006/`.
- DAT-007 completed: adopted `source-precedence/v1` now closes row eligibility, measurement-versus-projection, Group A/C incumbent review, Group B freshness, native-office and lineage-aware equal-vintage precedence, material-error/reference guards, GDP-growth comparability, and republication disclosure; every canonical API provenance entry carries a six-step decision trace, stable final tie-breaking removed database-order nondeterminism between UN and World Bank population rows, ten focused fixtures and all eight live worked examples pass, and the full 408-test claims/build/browser suite is green in commit `feat(reconciliation): publish deterministic decision traces (DAT-007)`; evidence: `plan/evidence/DAT-007/`.
- DAT-008 completed: `markSourcesSynced()` is the only sanctioned freshness writer and now fails closed for dry runs, zero or invalid row counts, blank source IDs, invalid timestamps, and executor failures; one normalized atomic update stamps successful row-writing runs, while the repository scanner covers every production source file and proves three seeded write violations are caught without flagging three safe controls, with 11 focused fixtures and the full production gate green in commit `fix(provenance): harden source freshness semantics (DAT-008)`; evidence: `plan/evidence/DAT-008/`.
- DAT-009 completed: `schema-data-dictionary/v1` documents all 49 Drizzle tables and 558 columns, including non-public tables so they cannot be mistaken for release data; every field materializes definition, exact SQL type, unit, null meaning, key/index/FK groups, source or derivation, cadence, vintage semantics, rights, release scope, and deprecation status, while a checked schema fingerprint and exact artifact comparison make the production build fail on schema/policy drift; six focused fixtures, TypeScript, ESLint, 425 tests, and the full build pass in commit `feat(data): publish schema data dictionary (DAT-009)`; evidence: `plan/evidence/DAT-009/`.
- DAT-010 completed: `derivation-version-envelope/v1` now gives Index dimensions/composites, Pulse v2 events/deltas, frozen reconciled facts, government taxonomy derivations, and release contracts a stable key plus explicit methodology, algorithm, prompt, taxonomy, source-basket, and source-ID states; new writers cannot emit guessed legacy versions, downstream rows preserve legacy input axes instead of substituting current versions, future exports require the envelope, and migration `0021` transactionally marked every existing live row `legacy_unversioned` with zero null envelopes; fourteen focused fixtures, writer/schema/migration validation, TypeScript, targeted ESLint, 439 tests, and the full build pass in commit `feat(research): retain row-level derivation versions (DAT-010)`; evidence: `plan/evidence/DAT-010/`.
- DAT-011 completed: the one named frozen release now has a self-hashed `raw-input-retention/v1` manifest joining four exact publisher-byte hashes and retrieval/adapter/rights records to all five released Index dimension groups and the composite dependency graph; publisher payloads remain excluded, reacquired bytes must match the frozen SHA-256 or reconstruction stops, semantic checksums and row counts detect parsed-output drift, and Atlas G2/Pulse remain explicitly unreleased rather than receiving invented capture history; seven focused fixtures, raw-retention/rights/version gates, targeted ESLint, 446 tests, and the full build pass in commit `feat(releases): retain immutable input reconstruction records (DAT-011)`; evidence: `plan/evidence/DAT-011/`.
- DAT-012 completed: all 45 registered production pipelines—35 scheduled and ten manual Atlas/Index/Conditions families—now have explicit zero-write dry runs, retry-safe canonical writers, and fixture evidence that repeated applications converge without duplicate rows; malformed, empty, duplicate, partial, and upstream-changed inputs fail before freshness, while 23 manual scripts are covered by a fail-closed contract guard; at least 131 focused checks, 580 tests, TypeScript, targeted ESLint, source/freshness/release/version gates, claims/docs, and the full production build pass in commit `fix(ingest): enforce repeatable production adapters (DAT-012)`; evidence: `plan/evidence/DAT-012/`.
- DAT-013 completed: one registry now closes 25 forward SQL artifacts and 11 operational data changes with honest journal status, restore/forward-compensation posture, zero-write planning, invariant commands, and internal release linkage; a checked live preflight records exact row counts for every affected relation across 36/36 artifacts with zero writes or missing relations, `db:push` refuses as production history, and eight adversarial fixtures, 588 tests, TypeScript, targeted ESLint, documentation, claims, and the full build pass in commit `fix(db): enforce migration change discipline (DAT-013)`; evidence: `plan/evidence/DAT-013/`.
- DAT-014 completed: one strict live command now checks all nine release-quality families and writes a checked anomaly report with repair instructions; twelve focused fixtures prove clean data passes and every seeded anomaly fails, while the live report honestly blocks release on one numeric parser corruption and three grouped statement-provenance orphan classes already assigned to DAT-029 and DAT-028; evidence: `plan/evidence/DAT-014/`.
- DAT-015 completed: one seven-state value-availability contract now preserves observed, missing, unknown, not-applicable, not-observed, disputed, and withheld distinctions across constrained storage, country APIs, Index rows, shared UI, indicator history, future export schemas, and the generated data dictionary; migration `0023` applied cleanly to 79,491 live rows with zero invalid combinations, seven end-to-end fixtures and 607/607 repository tests pass, and the production build and browser checks are green; evidence: `plan/evidence/DAT-015/`.
- DAT-016 completed: migration `0024` now records complete before/after history with reason, actor, operation, and time for every update or deletion across 29 evidence-bearing relations; Pulse negative decisions remain in the raw ledger, Pulse support/review rows cannot cascade away, and internal Pulse/reconciliation views expose retained error-study cases; seven adversarial fixtures, 614/614 tests, the live database validator, and the production build pass; evidence: `plan/evidence/DAT-016/`.
- DAT-017 completed: the frozen `atlas-2026-07-11` gzip release provides 253 stable jurisdiction records and 12,373 rights-cleared canonical facts from the immutable Q1 snapshot, each with its vintage, cutoff, selected source row, hash, method, and embedded source-specific rights; Index, Pulse, alternates, restricted sources, images, constitution text, and raw publisher payloads are excluded; evidence: `plan/evidence/DAT-017/`.
- DAT-018 completed: the public `civica-release-bom/v1` manifest now records the Atlas export source commit, Node/Next/Drizzle/TypeScript/tsx versions, four governing schema contracts, compressed and semantic file hashes and sizes, table counts, and per-source row counts, upstream vintage labels, observation-year bounds, retrieval cuts, and semantic hashes; live regeneration reproduces both normalized data and the BOM, four focused fixtures and 618/618 tests pass, and the manifest download, claims gate, and production build are green; evidence: `plan/evidence/DAT-018/`.
- DAT-019 completed: a public three-jurisdiction `civica-clean-room-fixture/v2` exercises the production Atlas export builder with permitted frozen canonical CIA Factbook, Wikidata, and World Bank rows; exact fixture/export hashes, row counts, rights, joins, vintage labels, cutoffs, and row hashes fail closed without a database or model key; the runbook limits the proof to legally shareable release tooling rather than claiming reconstruction of uncaptured publisher inputs; evidence: `plan/evidence/DAT-019/`.
- DAT-020 completed: `atlas-domain-coverage/v1` now publishes one generated operational dashboard and JSON surface for elections, constitutions, offices, people, parties, organizations, bills, indicators, and images, all scoped to the 194 `sovereign_state` rows; every domain exposes record and jurisdiction counts, measured field completeness, source families and per-source freshness, a domain run timestamp or an explicit unrecorded state, known gaps, reviewed thresholds, and derived alerts; the current snapshot honestly reports three current domains, six requiring attention, and eleven alerts, including six-jurisdiction bills coverage and the missing organization-run record; three adversarial fixtures, exact live regeneration, public/numeric/metadata/design gates, 622/622 tests, the production build, API checks, and dark/light/mobile browser review pass; evidence: `plan/evidence/DAT-020/`.
- DAT-021 completed: a direct-endpoint PostgreSQL 17 custom dump was created under enforced production read-only mode, restored into a loopback-only disposable cluster, and matched the 50-table schema plus exact counts and order-independent hashes for 253 jurisdictions, 25,827 fact observations, 56 source rows, and 7,891 provenance statements; a physical base backup and archived WAL then recovered to the named pre-mutation restore point, preserving the before-target marker and excluding the destructive after-target marker while retaining all source hashes; an available dump became queryable and verified in 3.319 seconds, the Atlas gzip/BOM restored with exact file and semantic hashes, four missing external-asset classes are documented, provider-specific Neon PITR and media policy remain in the manual queue, and every temporary dump/WAL/cluster was deleted; evidence: `plan/evidence/DAT-021/`.
- DAT-022 completed: `atlas-2026-07-11-g2-rc1` packages the frozen 253-jurisdiction/12,373-canonical-fact Atlas export with its exact export-builder source, BOM, snapshot-input manifest, rights manifest, codebook, coverage report, environment, citation draft, changelog, limitations, reproduction runbook, clean-room evidence, checklist, and checksums; the deterministic 1,869,727-byte archive has SHA-256 `978476a32355767eac6e73e792cd31580f850d9b498d576b4926fa4e10c9e53d`; the boundary covers immutable Civica vintage rows, not unretained publisher bytes or alternate observations; evidence: `plan/evidence/DAT-022/`.
- DAT-023 completed: migration `0025` repaired the sole Atlas cut so all 17,506 rows store the `v0.2-beta` version printed in their label, added hashes to all 237 named Index rows, and installed four database triggers that reject frozen updates/deletes and validate release-version and supersession lineage on insert; both writers now fail on changed same-label content, exact reruns are deterministic no-ops, and a corrected period requires a new version plus an explicit existing `supersedes_vintage_label`; rollback-only live mutation probes, 626/626 tests, all migration/data/claims gates, TypeScript, and the production build pass; evidence: `plan/evidence/DAT-023/`.
- DAT-024 completed: `civica-atlas-export/v3` exports 12,373 rights-cleared canonical rows from the immutable Q1 vintage instead of active `country_facts`; the snapshot supplies every citation-defining value/source/hash/version field and now separates observation year, upstream release, retrieval time, and Civica publication version; 161 selected source rows have current post-cut differences that the historical release excludes; the release family agrees on semantic SHA-256 `60556198b2ee3805f93558db47b1e5620c4f8f5cf372d6f83ebb6265fdcfa9fc`; evidence: `plan/evidence/DAT-024/`.
- DAT-025 completed: `temporal-metadata/v1` separates observation/reference year, upstream dataset release, retrieval time, and Civica publication version across the schema, frozen Atlas writer/export/manifests, peer-grouping API, generated dictionary, methodology, and BR/CGV reader view; the Jan26 QoG codebook proved the BR cross-section is reference year 2022, not the previously hard-coded 2025 time-series endpoint, so all 187 populated rows now retain 2022, original v6.1, QoG Jan26, the 2026 retrieval timestamp, and Civica `2026_v1` as distinct values; all 17,506 Atlas rows have a matching publication version, while unavailable historical source/retrieval metadata remains null rather than being inferred post-cut; 631/631 tests, the live temporal audit, release reconstruction, all gates, production build, API and desktop/mobile browser checks pass; evidence: `plan/evidence/DAT-025/`.
- DAT-026 completed: `drizzle/authoritative/` now supplies the sole deployable database history through a reviewed 50-table baseline, hash-pinned ordered manifest, external ledger, complete public-catalog fingerprint, and fail-closed `db:migrate`; an empty PostgreSQL 17 database and the production-shaped adoption path match fingerprint `92914965fc631e39e059fde8c7d9d4daaeb7cb24b8c025f19a28db60af9b6bee`, adoption replayed no public DDL or data writes, reruns have zero pending work, Vercel migrates before build, and 633/633 tests plus all live/static/build gates pass; evidence: `plan/evidence/DAT-026/`.
- DAT-027 completed: `country-research-export/v1` replaces the blocked mixed-source route with one resolver-selected canonical observation per exported fact and separate alternates, projections, and rejected evidence in semantically matched JSON/CSV; every row carries source URL/license, temporal metadata, value and lifecycle state, method, decision trace, and dispute state; source rights are applied after resolution so restricted rows never leak and a restricted canonical withholds the fact rather than promoting an alternate; France fixtures prove all four evidence classes and JSON/CSV population parity, the live France endpoints return 53 matching distributable observations with 36 honestly withheld fact keys, 636/636 tests and all browser/API/rights/build gates pass; evidence: `plan/evidence/DAT-027/`.
- DAT-028 completed: all 6,768 live provenance statements now resolve through a closed typed subject registry with zero orphan subjects and zero duplicate identity groups; term claims attach to terms, legislature-composition claims attach to government bodies, all seven producers use source-aware rerun identities, and authoritative migration `0001` adds database subject validation and uniqueness while retaining 5,365 updates and 1,123 deletions in research history; 636/636 tests, fresh-database rejection probes, exact live schema validation, all gates, and the production build pass; evidence: `plan/evidence/DAT-028/`.
- DAT-029 completed: the CIA numeric parser can no longer combine an unrelated prose year with a later scale word; registered fact-key envelopes preserve valid Pitcairn, Cocos, Vatican, Niue, and Tokelau edge values while the Atlas writer persists invalid candidates as rejected source evidence; authoritative migration `0002` quarantined the sole North Korea 2.01-trillion-percent parse, retained its full CIA prose/source and prior active state, and left no invented fallback; all nine live release-quality families now pass with zero active envelope violations, 641/641 tests and the production build pass; evidence: `plan/evidence/DAT-029/`.
- DAT-030 completed: the full Index refresh now stages the exact five-adapter source basket off-table, validates common version/period, minimum coverage, identity disjointness, required sources, and canonical checksum, then publishes dimensions, ingestion metadata, source freshness, and the terminal run manifest in one transaction; a real zero-write dry run staged 745 rows, a seeded V-Dem failure exited nonzero with zero score-history mutations and an unchanged 1,142-row/hash prior state, and the successful run published all 745 rows under checksum `9414401c2bcf097672af28bedcda924ad83ac29da5687d46188a76ea0a008f2c`; 644/644 tests, live ledger enforcement, all gates, and the production build pass; evidence: `plan/evidence/DAT-030/`.
- DAT-031 completed: the country list, country detail, and research export contracts now require an explicit `as_of=live` or complete immutable vintage label; live results carry no frozen label or cutoff, frozen results read only `country_fact_vintages` with no current-cache fallback, and both modes derive cutoff/retrieval/method metadata from their selected rows; missing, shorthand, and nonexistent selections fail with 400, a live database differential proves post-cut isolation, 647/647 tests and the production build pass; evidence: `plan/evidence/DAT-031/`.
- DAT-032 completed: reconciliation methodology v0.3-beta now stages and freezes the complete resolver input set with source/observation hashes, adapter and resolver code hashes, candidate status/payload, release checksums, and immutable winner pointers; the live Q2 cut contains 25,827 candidates and 17,515 winners, an exact rerun is a no-op, and a 4.9 MB local package reproduces both checksums with zero network requests; Q1 remains stable but is explicitly `canonical_only_legacy` rather than falsely replayable; 651/651 tests, live mutation/pointer checks, API/browser checks, all gates, and the production build pass; evidence: `plan/evidence/DAT-032/`.
- DAT-033 completed: CI ingestion records, CI dimension rows, Conditions rows, and indicator history now carry a nine-field source/indicator lineage contract; publisher hashes remain distinct from normalized-batch hashes, WGI substitution is explicit, and source-plus-indicator unique keys prevent cross-source replacement; 47,701 live records have valid lineage with zero invalid or duplicate identities, the 10/10 migration ledger, 654 tests, and the production build pass; evidence: `plan/evidence/DAT-033/`.
- IDX-001 completed: the current Index is consistently quarantined as a secondary research-beta experiment across visible navigation, page and structured metadata, API summaries and response metadata, claims, release/export policy, and atlas loading; APIs now state that independent validation is incomplete and the atlas does not depend on Index rows, while a dedicated five-surface regression suite, 656/656 tests, claims/API gates, production build, desktop/mobile browser checks, and the live local API pass; evidence: `plan/evidence/IDX-001/`.
- IDX-002 completed: `ci-beta-r3-2024-Q4` is the first current Index release certified through a clean-room replay of all four declared publisher snapshots, the checked 197-row jurisdiction spine, canonical adapters, PostgreSQL `real` round trips, ordered inputs, and deterministic per-jurisdiction seeds; every one of 745 live dimensions and 190 composites matches, including completeness, ranks, and interval endpoints, with zero unexplained rows; archived Beta/Beta-R1 defects and the superseded Beta-R2 audit release remain immutable; evidence: `plan/evidence/IDX-002/`.
- IDX-003 completed: `ci-missingness/v1` requires both mandatory dimensions and at least three of four overall, permits exactly one missing optional dimension as a labelled partial estimate, renormalizes only present weights, and withholds every two-dimension or mandatory-missing case; API metadata and strict schemas expose the policy, country and leaderboard UI name partial coverage, legacy six-dimension calculation is sealed to `v1.0`, all 190 live rows conform (175 full, 15 three-dimension partial, zero insufficient published), and clean-room reproduction remains exact. Beta-R4 publishes no generic uncertainty range; evidence: `plan/evidence/IDX-003/`.
- IDX-004 completed: Beta-R4 keeps the Index active but removes its unsupported same-spread Monte Carlo range and random point estimate; all 190 live scores are deterministic rounded weighted composites with null bounds, a checked four-source audit records 0/745 usable retained uncertainty rows and no covariance model, API and methodology wording match that estimand, clean-room reproduction is exact, 667 tests and the full production build pass, and browser checks confirm the current and partial-score surfaces; evidence: `plan/evidence/IDX-004/`.
- IDX-005 completed: Beta-R5 replaces arbitrary ID-broken ordinal ranks with competition ranking on the published integer estimate; equal scores share rank, deterministic ID ordering is nonordinal, all 190 live scores conform across 61 tied score groups, API/methodology/UI expose the policy and explicitly decline unestimable rank intervals, clean-room reproduction is exact, 669 tests and the production build pass, and browser checks announce tied ranks accessibly; evidence: `plan/evidence/IDX-005/`.
- IDX-006 completed: `ci-research-panel-2000-2024-v1` freezes 24,250 private jurisdiction-year-indicator cells across 194 current sovereign jurisdictions, 25 years, and five source-native series; 19,866 observations and 4,384 explicit gaps carry source, vintage, unit, transform, uncertainty, revision, and missingness metadata with no carry-forward or freshest-value substitution; immutable database triggers, deterministic row/coverage/break hashes, aggregate checked artifacts, 672 tests, live mutation probes, methodology browser verification, and the production build pass; evidence: `plan/evidence/IDX-006/`.
- IDX-007 completed: `civica-original-measurement-charter/v1` defines target users and permissible decisions, units and evidence cadence, provenance-native and meta-measurement construct boundaries, descriptive nonclaims, two-part novelty, simple and no-score baselines, misuse controls, and automatic retirement; a machine-readable contract and validator enforce the no-winner and no-incumbency rules, and all 674 tests pass; evidence: `plan/evidence/IDX-007/`.
- IDX-008 completed: `civica-index-candidate-set/v2` specifies six materially distinct candidates—dashboard/no-score, hardened composite with exact PR+CL input, measurement concordance, power-transfer ledger, constitution/practice pairings, and institutional constraint map—with complete input, transform, missingness, uncertainty, version, normative, value, presentation, validation, and retirement contracts; seeded validation rejects cosmetic duplicate sets; evidence: `plan/evidence/IDX-008/`.
- IDX-009 completed: the candidate validator now fails unless the set retains a provenance-native disagreement or institutional-fact alternative and a separate institutional-structure alternative, and it verifies that every such candidate explicitly forbids a hidden quality grade, aggregation, score, or rank; Measurement Concordance, Power and Transfer Ledger, and Institutional Constraint Map satisfy the closed gate; evidence: `plan/evidence/IDX-009/`.
- IDX-010 completed: timestamped `civica-index-tournament-preregistration/v2` supersedes v1 before outcome inspection and freezes corrected panel/candidate/code hashes, untouched temporal and deterministic geographic holdouts, six candidates and baselines, noncompensating gates, thresholds, subgroup/sensitivity work, missingness/exclusions, multiplicity, amendment handling, simplicity, and no-winner rules; evidence: `plan/evidence/IDX-010/`.
- IDX-011 completed: `civica-index-baselines/v2` implements the full-grid dashboard/no-score, native V-Dem, complete-case equal-weight common-scale, and development-only first-factor baselines against corrected panel v2; it produces 4,850 B0, 4,298 B1, and 2,206 B2/B3 rows with deterministic output/model hashes and live exact reproduction; evidence: `plan/evidence/IDX-011/`.
- IDX-038 discovered: K1's canonical Freedom House input is the 2–14 `pr_cl_total` ratings sum, but panel v1 used the distinct 0–100 `fh_total_score`; panel v2 now preserves the exact hash-verified publisher input without rewriting v1, while preregistration and baseline advancement remain before the task can close.
- IDX-038 completed: immutable panel v2 replaces the mismatched Freedom House total-score series with the exact hash-pinned PR+CL ratings input used by K1, preserves 2000–2005 as structural gaps and all v1 artifacts as history, advances the candidate set, preregistration, and baselines to v2 before outcome inspection, and passes exact publisher/live reproduction; evidence: `plan/evidence/IDX-038/`.
- IDX-038 reopened: a second input-identity audit found that v2 still omits K1's disclosed WGI Voice & Accountability fallback when V-Dem is absent; completion now requires the exact fallback series, precedence rule, and new frozen panel/protocol/baseline releases.
- IDX-038 completed after re-audit: panel v3 adds exact hash-pinned WGI Voice & Accountability beside V-Dem and fixes V-Dem-first fallback precedence without conflating WGI Rule of Law; candidate, preregistration, and baseline releases all advance to v3, B2/B3 coverage grows to 2,270 valid units, prior versions remain immutable, and live publisher reproduction passes; evidence: `plan/evidence/IDX-038/`.
- IDX-012 completed: `k1-current-composite-tournament/v1` isolates the current four-dimension method with exact source-indicator precedence and aliases, production transforms and weights, current missingness, no unsupported interval, shared splits, and competition ranks; panel v3 yields 3,659 hashed outputs (2,270 full, 1,389 partial), and the live validator reproduces all 190 Beta-R5 scores, completeness flags, missing sets, and ranks exactly; TypeScript and 688 tests pass; evidence: `plan/evidence/IDX-012/`.
- IDX-032 completed: `k2-measurement-concordance/v1` computes 3,260 private three-rater common-coverage profiles with named percentiles, range/IQR, explicit uncertainty absence and source-dependence caveats; development midpoint artifact R² is 0.098 but drop-one-rater tercile instability is 65.65%, so the highlighted summary remains fragile while holdouts and expert known-case labels stay sealed; live reproduction, TypeScript, and 690 tests pass; evidence: `plan/evidence/IDX-032/`.
- IDX-033 completed: `k3-power-transfer-ledger/v1` produces 168 private, statement-cited current-executive rows (117 observed identities, 51 contested) while honestly computing zero historical transfers and zero term-limit states from the incomplete current history; a public rulebook covers identity, alternation, coalitions, indirect/interim/collective/disputed cases, tenure, term limits, missingness, and nonclaims, the validation protocol is frozen, live reproduction and claims/design gates pass, and desktop/mobile browser checks confirm the canonical methodology layout; evidence: `plan/evidence/IDX-033/`.
- IDX-034 preflight recorded: current constitutional excerpts are usable, but the only available external series are broad LDI, Freedom House, WGI Rule of Law, CPI, and HDI measures that cannot fairly represent a specific constitutional commitment; four tempting mappings are explicitly rejected, K4 emits no false pairing, and new prerequisite IDX-039 owns practice-specific input ingestion.
- IDX-039 completed: immutable `ci-k4-practice-panel-2000-2024-v1` freezes three codebook-justified V-Dem v15 practice measures with exact archive/codebook hashes, definitions, native units, publisher credible bounds, explicit gaps, temporal caveats, and private-rights posture; 12,867 of 14,550 cells are observed, 1,683 remain missing without imputation, broad composites are forbidden substitutes, and live hash/immutability validation passes; evidence: `plan/evidence/IDX-039/`.
- IDX-034 machine prototype completed, external gates pending: K4 reproducibly joins 582 jurisdiction-construct candidates, preserves full constitutional excerpts and V-Dem credible bounds, and structurally forbids gap/hypocrisy/score/rank outputs; 329 rows have candidate excerpts, 253 have no tagged excerpt, and all remain provisional until the frozen two-coder alpha and constitutional-scholar fairness gates pass; evidence: `plan/evidence/IDX-034/`.
- IDX-013 K5 implementation completed, shared-interface gate pending: `k5-institutional-constraint-map/v1` reproducibly extracts 2,138 private relation candidates across 181 jurisdictions and the frozen geographic splits; ontology tags assert zero graph edges, unknown endpoints remain unknown, held-out labels are untouched, prohibited quality/count/rank outputs are absent, and double coding/expert/citation gates remain pending; evidence: `plan/evidence/IDX-013/K5-README.md`.
- IDX-013 completed: K0–K5 now run as isolated implementations under `civica-tournament-evaluation-interface/v1`, which standardizes unit/output kind, frozen inputs, split and missingness accounting, evidence coverage, uncertainty posture, hashes, label access, and validation state without flattening candidate semantics; K3/K4 now carry the preregistered geographic split, every candidate/baseline reproduces, held-out outcome metrics remain sealed, and the suite hash is `9f82c970c1aa4d8b94702bf3da5d1a2c4ae7d10b9e37a8e3477bd8ef471759ca`; evidence: `plan/evidence/IDX-013/`.
- IDX-014 completed: correlation PCA over 2,270 complete panel-v3 profiles reports full matrices/eigenvalues/loadings across pooled, between-country, within-country, annual-change, yearly, time-block, continent, and current-regime slices; PC1 explains 87.3% of pooled levels and 88.1% between countries but only 52.5% within countries and 35.9% of annual changes, so the level factor cannot validate a single change construct; all JSON/CSV/SVG artifacts byte-reproduce at analysis hash `07b3b8b5513622fb3a7f3fabb8e8e811d0f55ae4f7a642f7f1681cfeb8ddf954`; evidence: `plan/evidence/IDX-014/`.
- IDX-015 preregistered: five frozen hypotheses use HDI as a limited external correlate for K1/K2 level, annual, change, and undesired-association checks, while all constituent-source correlations are explicitly mechanical and unable to count as validity; K3–K5 remain insufficient pending construct-matched external labels; no validity correlations have been calculated under the protocol yet.
- IDX-015 completed: the preregistered HDI checks report K1 country-mean rho 0.667 (95% cluster interval 0.574–0.747), median annual rho 0.658 (0.554–0.742), K1/HDI change rho 0.019 (−0.027–0.064), and K2-spread/HDI rho −0.078 (−0.186–0.036); input correlations are separately marked mechanical and cannot pass validity, while K3–K5 remain insufficient pending construct-matched external labels; evidence: `plan/evidence/IDX-015/`.
- IDX-016 preregistered: K1 will be predicted on a common complete final-holdout sample by development-fitted V-Dem, equal-weight, first-factor, and four-public-input OLS comparators; R²/RMSE/MAE and 2,000 jurisdiction-cluster bootstraps are frozen, R² ≥ 0.90 for public-input reproduction fails originality, human task utility remains separate, and K2–K5 stay insufficient pending construct-matched benchmarks.
- IDX-016 completed: on 901 complete final-holdout rows, the four-public-input model reproduces K1 at R² 0.999847 (95% cluster interval 0.999809–0.999873), the equal-weight mean reaches 0.999770, the first factor 0.994664, and V-Dem alone 0.928526; K1 fails the frozen original-information gate, decision utility remains untested, and K2–K5 retain insufficient benchmark states; evidence: `plan/evidence/IDX-016/`.
- IDX-040 completed: immutable longitudinal-label v2 freezes 4,357 observed BR/CGV country-years and 105 explicit gaps from exact QoG Jan26 bytes, while Jan24/Jan25 QoG and V-Dem v14/v15 hashes support real revision checks; the zero-observation v1 parser attempt remains immutable failed history, v2 corrects `0.0/1.0` parsing under a new release, and live coverage/hash/immutability validation passes; evidence: `plan/evidence/IDX-040/`.
- IDX-017 preregistered: frozen BR transitions drive signed t−1→t+1 responsiveness and −2…+2 lead/lag tests, unchanged BR periods define quiet-noise controls, exact QoG Jan24/25/26 event sets and V-Dem v14/v15 inputs drive revision sensitivity, thresholds and 2,000 jurisdiction-cluster bootstraps are fixed, and all missing endpoints remain excluded without imputation.
- IDX-017 completed: 55 usable BR transitions yield 74.5% K1 direction accuracy (95% cluster interval 63.3%–85.0%), five-point median signed movement, and a transition-year response peak; 2,890 quiet periods have one-point median and four-point p95 movement; isolated V-Dem revision shifts pass at median zero/p95 one/max two, while BR label stability visibly fails Jan24→Jan26 at 84.9% common-window Jaccard and passes Jan25→Jan26 at 99.2%; evidence: `plan/evidence/IDX-017/`.
- IDX-018 completed: the frozen final folds confirm K1's originality failure at R² 0.999847 (95% interval 0.999809–0.999873) and K2's stability failure at 63.2% drop-one-rater change (56.0%–70.0%); K3–K5 retain insufficient-label states, and all geographic region/regime cells in the 25-jurisdiction holdout are below n=30 so performance estimates are suppressed; evidence: `plan/evidence/IDX-018/`.
- IDX-041 completed: immutable uncertainty v2 freezes exact publisher points plus 172 V-Dem credible regions, 192 WGI Voice and 192 WGI Rule-of-Law intervals, 177 CPI confidence bounds, and explicit zero-bound Freedom House coverage across a 970-cell grid; the six v1 republisher/publisher point-bound mismatches remain preserved failed evidence, all 733 v2 bounded points lie inside their own publisher intervals, and the release cannot be called a calibrated composite confidence interval; evidence: `plan/evidence/IDX-041/`.
- IDX-019 completed: the reproducible grid varies every declared input, source, vintage, normalization, weight/model, aggregation, missingness/imputation, publisher-bound dependence, outlier, fold, and coverage family; dimension removal drives p95 rank shifts of 23–33 places, normalization/median aggregation 14, WGI substitution 11, near-equal weight variants three, and bound stresses roughly three to four, while imputation adds one country but drops top-decile Jaccard to 0.739; evidence: `plan/evidence/IDX-019/`.
- IDX-020 completed: the source map proves direct V-Dem/Freedom House reuse inside WGI and shared Freedom House/EIU/Bertelsmann families across WGI and CPI; publisher-level deletion moves some ranks sharply, true upstream-family deletion is unidentifiable from the aggregates, and an enforced claim rule bars independent-corroboration or original-measurement language; evidence: `plan/evidence/IDX-020/`.
- IDX-021 completed: all eight frozen subgroup families now report publication, score distribution, publisher-bound coverage, scarcity simulation, sample suppression, and explicit unestimability; 193/194 sovereign states publish, masking CPI raises the complete-case median by one with 33.9% moving down, so K1 clears only the narrow mechanical scarcity rule while subgroup accuracy remains unclaimed; evidence: `plan/evidence/IDX-021/`.
- IDX-022 prepared but remains open: the randomized qualified-reader crossover, six task constructs, exclusions, paired outcomes, noncompensating utility thresholds, tie-to-K0 rule, and strict prohibition on treating agent dry runs as G5 evidence are frozen before responses; execution awaits the IDX-031 dashboard fixture and qualified humans; evidence: `plan/evidence/IDX-022/PREPARATION.md`.
- IDX-023 completed: eight adversarial threat families now bind evidence to mitigations and suspension/retirement triggers; the current default K1 league-table presentation fails misuse resistance, while the underlying candidate remains versioned research pending the formal disposition rather than being silently retired; evidence: `plan/evidence/IDX-023/`.
- IDX-031 completed: the Governance Evidence Dashboard presents the five frozen 2024 publisher observations for 194 sovereign states on their native scales with owner, construct, direction, vintage, source link, published interval or explicit absence, missingness, and point-of-use rights; its rights-safe download retains only export-authorized WGI observations, source-file fixtures reproduce all 970 cells exactly, no composite/grade/rank is present, and desktop browser checks confirm Japan and Uruguay; evidence: `plan/evidence/IDX-031/`.
- IDX-024 completed: one rights-safe tournament package now regenerates K0–K5, all frozen baselines, the shared evaluation envelope, and every registered confirmatory analysis through 17 logged stages; its 22-artifact inventory locks the v3 protocol and panel, code tree, environment, seeds, results, tables, figure, and nine failure/pending states, while an empty exploratory register and `winnerSelected: false` prevent post hoc additions or premature disposition; evidence: `plan/evidence/IDX-024/`.
- IDX-025 completed: the confirmatory decision applies all 24 frozen thresholds without compensation—eight pass, K1 originality and K2 stability fail, and 14 human/expert thresholds remain insufficient—so no candidate wins; K1's original-measurement claim is rejected while bounded derivative utility remains unresolved, the simplicity rule is recorded but untriggered, and any threshold change is forced into a non-winning exploratory release; evidence: `plan/evidence/IDX-025/`.
- IDX-026 completed: `civica-index-disposition-2026-07-v1` selects the source-native Governance Evidence Dashboard as the public comparison product, preserves K1 as nonrecommended versioned research rather than permanently retiring it, rejects original-measurement and validated-index claims, records failed and unresolved tests plus the case for continued utility research, and publishes exact reconsideration criteria on the methodology page; stale analysis/API/replication prose was corrected and all 721 claims tests plus browser checks pass; evidence: `plan/evidence/IDX-026/`.
- IDX-027 completed: the adopted source-native disposition now governs homepage/navigation, country data, compare, rankings, Atlas, sitemap/redirects, metadata, general-country API, legacy Index APIs, replication status, methodology, claims, rights, and embeds; composite code and frozen releases remain preserved research, six score endpoint families announce a 2026-07-31 sunset and fail with 410 afterward, old widgets fail closed, the public Atlas and general country API no longer load composite data, and a disposition-wide validator, 723 tests, claims gate, production build, and browser checks pass in commit `product: migrate public Index surfaces to governance evidence`; evidence: `plan/evidence/IDX-027/`.
- IDX-028 completed: `governance-evidence-review-packet-2026-07-v1` binds the selected source-native construct, frozen input manifest, implementation/environment, five-row codebook, transformations, uncertainty, validation, sensitivity, subgroup limits, limitations, citation, exact reproduction workflow, ten neutral questions, and the complete no-winner tournament/disposition/misuse record in one 46-artifact rights-safe inventory; semantic hash `6becae1710d78ae7e3e79ca614b2a48d670961979fa7914a35f453e3c36ceec1`, 726 tests, claims gate, production build, and browser inspection pass; evidence: `plan/evidence/IDX-028/`.
- IDX-029 completed: `civica-index-research-archive-2026-07-v1` physically freezes the exact 72-file tournament source tree, binds all 22 rights-safe artifacts, retains K1/K2's two failed thresholds plus 14 null/insufficient results, records non-recommendation reasons for K1–K5, and enforces a five-step new-version/disposition protocol against silent revival; archive semantic hash `4ae82eda809342a44574a3cf3142ecc6df440f2047b64a5b268114f2112b465a`; evidence: `plan/evidence/IDX-029/`.
- IDX-030 completed: `civica-index-change-control/v1` establishes append-only snapshot `dd14ea135ffcd0cd1f8ae15d9f437ae29d312272d4ac9118e63904f5c11909cb` over 63 classified Index semantic files; any protected drift requires a new version plus changed docs, registry, release note, migration plan, golden test, and contract test, while CI reruns ten declared validators and model/transform changes require an additional new version-specific validator; five negative/positive fixtures, 733 tests, and the dynamic validation runner pass; evidence: `plan/evidence/IDX-030/`.
- IDX-035 completed: `civica-index-research-selection-2026-07-v1` closes Beta-R3/R4/R5 selection over release, method, quarter, source, indicator, artifact, input transform, composite algorithm, fallback priority, and display transform; calculations, peer/research reads, and deprecated APIs now reject mixed identities, an overlapping-release fixture selects only the requested method, and append-only change-control snapshot `d9b04e845ff3e5ca89f4bcfbe094ee526f5fef32c30271de13dea2f67feb5ea7` protects 75 semantic consumers; evidence: `plan/evidence/IDX-035/`.
- IDX-036 completed: live calculation clocks prove that all 1,236 rows across eight stored 2023-Q4/2024-Q4 Index method groups were computed in 2026; the `as_published_release`/`harmonized_backcast` contract now separates reference period, publication cut, calculation time, method, and citation in API/UI/export schemas, rejects false historical labels, and leaves the genuinely unavailable as-published series explicit rather than manufacturing one; change-control snapshot `5fba22427074efdc3743391cddb877b82b23e3c46d064c9ce72e3b9661be50cc`; evidence: `plan/evidence/IDX-036/`.
- IDX-037 completed through the declared claim-narrowing path: the n=46 2023 PCA is now a historical squared-loading weight recipe rather than evidence for a universal or longitudinal governance factor; the later four-input temporal result is linked as separate level/change evidence, Administrative Capacity remains explicitly untested with no five-variable rotation, and a fail-closed public-surface validator prevents revival of the removed claims; change-control snapshot `246141a41b81fed1f5cd7e7911da51993b22925f227c3eeeb62cf6e9c3a9efce`; evidence: `plan/evidence/IDX-037/`.
- PUL-001 completed: Pulse numeric deltas remain preserved research but are now governed by `pulse-numeric-publication/api-only-experimental-v1`; live reader routes and bulk exports omit them, the versioned API labels them experimental under `pulse-v2.1-beta` with validation limitations and no scalar score, both the API-only and full-omission policies are snapshot-tested, and a fail-closed validator prevents stronger or wider publication; evidence: `plan/evidence/PUL-001/`.
- PUL-002 completed: `pulse-ledger-charter/v1` defines one evidence-linked domestic institutional event record as the unit, closes users and prohibited uses, six inclusion and six exclusion rules, source and geographic/temporal boundaries, observability and non-claims, noncompensating success/suspension/retirement criteria, and a no-value outcome; the citable public section and adopted resolution are machine-validated against a live 384-event/89-jurisdiction adoption snapshot; evidence: `plan/evidence/PUL-002/`.
- PUL-003 completed: `pulse-event-ontology/v3.0` carries all 61 production concepts into a five-dimension multi-label research codebook with facet-specific evidence, separate direction and nonnumeric severity, explicit compatibility and abstention rules, lawful/ambiguous-event guidance, six executable examples/counterexamples, and major-version migration policy; the public methodology distinguishes adoption from the still-v2.0 production runtime, 756 tests and all claims/design/change-control gates pass, and desktop/mobile light/dark browser checks show no overflow or console errors; evidence: `plan/evidence/PUL-003/`.
- PUL-004 completed: `pulse-pipeline/versioned-lineage-v1` gives every attempted stage an immutable content-addressed method envelope and links raw items, clusters, classifications, reviews, events, and dimensional outputs to the applicable runs; two authoritative migrations preserve retained rows under six explicit legacy identities without inventing modern versions, live coverage has zero missing required links, mixed/legacy API sets cannot claim one comparable series, 760 tests and all migration/claims/design/API gates pass, and desktop/mobile light/dark browser checks confirm the public version record; evidence: `plan/evidence/PUL-004/`.
- PUL-005 completed: `pulse-raw-evidence/v1` seals exact item URL/retrieval time, private fetched evidence, content and identity hashes, language state, publisher/source family, ingest attribution evidence, and captured rights on every raw item; all event-source rows require that link, APIs expose safe identity metadata without publisher payload, the live 16/16 migration audit closes 1,379 snapshots and 529 links with zero gaps or mismatches, 764 tests and all claims/rights/retention/API gates pass, and desktop/mobile light/dark browser checks confirm the public evidence record; evidence: `plan/evidence/PUL-005/`.
- PUL-006 completed: `pulse-cluster/normalized-global-union-find-v3` removes the ingest-country bucket, combines multilingual semantic and canonical-token identity with a conservative anchor guard, and groups classification by event cluster before subject attribution; cross-family English/Spanish fixtures merge while distinct same-day rulings stay separate, the live stage assigns all 203 formerly excluded reports into 191 versioned clusters without classifying or publishing them, the stable coverage API separates 915 legacy from 191 v3 clusters and exposes the severe source-concentration limitation, 771 tests and the full build pass, and desktop/mobile light/dark checks show no overflow or browser errors; evidence: `plan/evidence/PUL-006/`.
- PUL-007 completed: `pulse-source-independence/evidence-family-v1` collapses same-publisher, wire, mirror, named-origin, canonical-URL, and near-verbatim dependencies before corroboration; the frozen 12-pair regression fixture reaches 1.00 precision/recall against preregistered 0.95/0.90 gates, the live audit reduces 529 reports to 439 evidence groups across 384 events, versioned production recomputation updates 384 confidence rows and 325 API-only experimental dimension rows without publishing events, 774 tests and the full build pass, and desktop/mobile light/dark checks show no overflow or browser errors; evidence: `plan/evidence/PUL-007/`.
- PUL-008 completed: `pulse-source-coverage/v1` derives operating, degraded, and inactive feed states from retained per-connector retrieval telemetry, evidence, source-input registration, and rights rather than code presence; the shared API, methodology, and admin dashboard expose success/failure, attempts, yield, latest data, language and ISO3 scope, unresolved rows, rights, and blind spots; a live run inserted 128 GDELT rows and reports four operating and six inactive feeds with no stub promoted, 778 tests and the production build pass, and desktop/mobile light/dark checks show no overflow or browser errors; evidence: `plan/evidence/PUL-008/`.
- PUL-009 completed: `pulse-observability/country-period-v1` separates four observation states from three event-observation outcomes in the country-dimensions API; no-event language requires the declared two-feed/five-document operational threshold, low coverage and current outage remain not assessable, restricted-information status requires future sourced context rather than the approximate fallback, and strict cross-field schemas reject any absent-event numeric delta or country-quality inference; live checks exercise sufficient/no-event, low/not-assessable, and low/event-observed combinations, 786 tests and the production build pass, and desktop/mobile light/dark methodology checks render cleanly; evidence: `plan/evidence/PUL-009/`.
- PUL-010 completed: `pulse-information-environment-context/v1` removes the approximate static country table and midpoint fallback, preserves missing context as null with no production weighting effect, and records the exact official RSF candidate release, hash, vintage, coverage, methodology, terms, and disabled rights posture without redistributing country values; the legacy multiplier policy is sensitivity-only and explicitly uncalibrated, public APIs hide historical raw scalars, the append-only shared contract, 794 tests, claims/design gates, production build, live API checks, and light/dark browser review pass, and browser QA also removed visible internal claim comments from reader markdown; evidence: `plan/evidence/PUL-010/`.
- PUL-011 completed: `pulse-decision-ledger/v1` stores event existence, subject attribution, category, severity, calibration, corroboration, and publication as independent append-only judgments; generic confidence is prohibited, verifier and manual refutations preserve same-axis history, 2,688 live legacy decisions are explicit projections rather than invented verification, 799 tests and all migration/runtime/claims/design/build gates pass, and light/dark browser checks confirm the public contract; evidence: `plan/evidence/PUL-011/`.
- PUL-012 completed: `pulse-jurisdiction-attribution/v2` stores one primary plus affected jurisdictions with role-specific rationales, evidence references, and frozen entity snapshots from a content-hashed 253-row catalog and versioned aliases; unresolved results block auto-publication, the API exposes the requested country's role, all 384 retained events remain honest legacy projections, the rolled-back cross-border database fixture proves one-primary/one-affected materialization and append-only enforcement, 801 tests and the full build pass, and responsive light/dark browser checks are clean; evidence: `plan/evidence/PUL-012/`.
- PUL-013 completed: `pulse-candidate-outcome/v1` retains duplicate, non-event, insufficient-evidence, invalid, refuted, and rejected candidates with reason, actor, method, run, time, and evidence; ingestion duplicates are now individually recorded, decision outcomes materialize transactionally, the stable evaluation view exposes direct false-positive/false-negative sampling strata, historical aggregate duplicates remain honestly unreconstructed, and live rollback/immutability fixtures pass; evidence: `plan/evidence/PUL-013/`.
- PUL-014 completed: `pulse-evaluation-sampling-frame/v1` freezes one 384-event-candidate census and two design-inflated probability frames over 978 negative/unresolved candidates and 17,460 country-days before gold-label access; deterministic stratification, reserves, primary base weights with calibrated-margin analysis, geography/time/language/source/regime/media-evidence margins, benchmark/generalization distinction, and the regression-only famous-case boundary are executable; evidence: `plan/evidence/PUL-014/`.
- PUL-015 completed: `pulse-country-day-evaluation-set/v1` freezes 536 unlabeled country-day packets spanning 181 jurisdictions, with 482 analysis candidates, 54 reserves, 35 retained-evidence packets, and 1,608 rights-safe date-bounded query traces; exact population/sample/trace linkage, calibrated-weight posture, label-leak guards, 811 tests, the claims gate, and the 98-page production build pass; evidence: `plan/evidence/PUL-015/`.
- PUL-016 completed: `pulse-independent-coding/v1` freezes a 61-category operational boundary catalog, six worked examples, 12 answer-free blind packets, structured uncertainty and abstention, immutable raw submissions, and separate non-majoritarian adjudication; two independent GPT-5.3 Codex Spark dry runs exposed and repaired three instruction/schema defects, then passed every validator while preserving three disagreement packets as non-gold diagnostics; 815 tests, the claims gate, and the 98-page production build pass in commit `research: define independent Pulse coding protocol`; evidence: `plan/evidence/PUL-016/`.
- PUL-017 completed: `pulse-coding-workspace/v1` adds a separate role-gated double-coding application with frozen evidence packets, coder-only sessions, permanently hidden peer labels, database-immutable locks, post-lock comparison, separate non-majoritarian adjudication, status-only active-study owner access, and stable credential-free audit exports; a 12-packet live dry pilot preserves three unresolved disagreements, temporary access was revoked, browser QA passed at desktop/mobile widths, all 823 tests and the production build pass; evidence: `plan/evidence/PUL-017/`.
- PUL-039 completed: `pulse-independent-coder-recruitment/v1` prepares four-coder/two-adjudicator roles, blindness and conflict rules, paid training and qualification, six contact-free sourcing pools, draft outreach, six-to-eight-week panel lead time, and executable workload/budget scenarios over 1,456 initial units and 2,912 assignments; the base estimate is $45,615 pending owner approval and a paid human timing pilot, nobody was contacted, and external contact remains blocked until G4 even though PUL-041 later completed the packet prerequisite; evidence: `plan/evidence/PUL-039/`.
- PUL-041 completed: `pulse-evaluation-packet-manifest/v1` freezes the 384-item event-candidate census and a 536-item system-negative probability draw with evidence/search context, design weights, codebook/ontology hashes, and no publisher payload or production/model/owner answer fields; all 920 packets were idempotently rehydrated into two neutral workspace studies that remain disabled with zero participants or assignments, and static/live validators pass; evidence: `plan/evidence/PUL-041/`.
- PUL-025 completed: `pulse-validation-protocol/v1` separates ten noninferential historical regression fixtures, the frozen 384/536/536 retrospective evaluation frames, and a 90-day prospective shadow window; the protocol requires the current ensemble and full ingest-to-publication pipeline, retains spurious dimensions and missed/retrieval failures, forbids tuning after future labels, remains explicitly not started, and is protected by Index/Pulse change-control v23; evidence: `plan/evidence/PUL-025/`.
- ATL-001 completed: `civica-atlas-surface-data-matrix/v1` binds 37 public route/module rows—14 top-level data surfaces, the shared country masthead, 13 Factbook sections, the Factbook source/citation block, seven Civica Data modules, and the constitution reader—to their renderers, queries, storage/fields, provenance, coverage, seven data/UI states, tests or explicit gaps, owners, and frozen-export relation; evidence: `plan/evidence/ATL-001/`.
- ATL-002 completed: `atlas-domain-coverage/v1` now computes records, sovereign-state coverage, important-field completeness, source families, freshness, known gaps, alerts, and release-readiness behavior for all 14 declared Atlas domains; five currently meet the declared 80% coverage/completeness and 180-day freshness minimums, nine remain visibly `Attention` with 14 concrete alerts, no missing timestamp was inferred, and the API and reader page preserve every below-threshold domain instead of describing it as complete; live audit, 931 tests, design-token validation, desktop/mobile light/dark browser QA, and the 105-page production build pass; evidence: `plan/evidence/ATL-002/`.
- ATL-003 completed: country Civica Data and Compare now publish five documented publisher-native longitudinal series with source/release/artifact/transformation lineage, native units and scales, explicit absence states and long gaps, series/time controls, and rights-filtered JSON/CSV downloads; live coverage spans 46,215 observations, the Japan query executes in 0.725 ms, browser QA passes across desktop/mobile and both themes, the G2 and reviewer packet inventories were refreshed without changing released observations, all 936 tests pass, and the 105-page production build passes; evidence: `plan/evidence/ATL-003/`.
- ATL-004 completed: every shared fact-source panel now labels single-source, within-tolerance agreement, or resolver-selected disagreement; shows selected and alternate values with dates/vintages; collapses republishers into verified producing families while excluding compilations/unknown lineage from corroboration; and exposes the exact six-step deterministic resolver trace; Argentina desktop/mobile light/dark browser QA, 23 focused tests, and all 939 claims tests pass; evidence: `plan/evidence/ATL-004/`.
- ATL-005 completed: every country Civica Data page now opens with a faceted evidence-coverage view that reports DAT-005/006 coverage, missingness, source-family depth, freshness, disputes, and eligible multi-source resolver agreement without a score or country judgment; evidence: `plan/evidence/ATL-005/`.
- ATL-006 completed: the sourced `jurisdiction-status/v1` contract now governs the full 253-entry reference catalog across directory/search/Compare/sitemap, country and Compare disclosures, API filters and responses, research exports, Atlas scope, metadata, and Country-versus-Place JSON-LD; politically sensitive fixtures, desktop/mobile browser QA, 949 tests, Index change-control v26, and the 105-page production build pass; evidence: `plan/evidence/ATL-006/`.
- ATL-007 completed and corrected: `election-corpus-audit/v1` accounts for all 915 retained rows, qualifies 572 conceptual events plus 25 additional chamber contests, quarantines 88 weak or conflicting-scope rows, verifies 877 publisher jurisdiction assignments with 21 missing and 17 conflicting evidence rows, separates 17 tentative source dates from 168 reader-facing projections, binds public reads to exact row/result/statement/source content, and keeps pending-rights sources out of bulk export; the P17/P1001 scope correction, live fingerprint, 977 tests, Index change-control v28, browser QA, and the 106-page production build pass; evidence: `plan/evidence/ATL-007/`.
- ATL-008 completed: the election calendar and 555-event historical timeline use the checked live-fingerprint-qualified corpus, keep 25 chamber contests attached to their conceptual events, expose resultless and sparse jurisdiction states, disclose date-only/no-timezone semantics and tentative dates, require exact IPU evidence for electoral-system labels, isolate Compare/country/system outage states, and publish a filterable JSON/CSV API that emits only rights-cleared Wikidata rows while naming withheld IPU/IDEA/projection fields; browser QA, 977 tests, Index change-control v28, refreshed G2/Atlas/Index review packets, and the 106-page production build pass; evidence: `plan/evidence/ATL-008/`.
- GOV-008 completed: `civica-reviewer-selection/v1` adopts names-free, pre-contact criteria for governance measurement, political event data, research-data curation, accessibility, and legal/source-rights review, with exact expertise evidence, three conflict outcomes, geographic/method diversity, bounded deliverables, exclusions, professional accessibility/legal boundaries, and a validator proving that no names or private contact data entered the policy; no candidate was contacted; evidence: `plan/evidence/GOV-008/`.
- GOV-009 completed: `civica-reviewer-longlist/v1` independently checks 24 public-evidence candidates—eight each in governance measurement, political event data, and research-data curation—with current role/work, method contribution, exact fit, provisional conflicts, geographic/method perspective, source links, and public professional channels; direct contact data and availability guesses are excluded and nobody was contacted; evidence: `plan/evidence/GOV-009/`.
- GOV-010 completed: `civica-reviewer-ranking/v1` applies a documented seven-axis rubric to all 24 verified candidates, keeps availability and communication burden explicitly unknown rather than guessed, retains three proposed primaries, three alternates, and two reserves per core lane, preserves non-affiliated judgment for source conflicts, and queues owner approval while contact remains blocked; evidence: `plan/evidence/GOV-010/`.
- GOV-011 completed: `civica-reviewer-dossiers/v1` generates 18 one-page, candidate-specific dossiers for every proposed primary and alternate, each with one bounded question, versioned artifacts, hours and deliverable, conflict/recusal and publication terms, honorarium-pending posture, and a respectful unsent draft that makes declining easy; Pulse asks wait for GOV-015, nobody was contacted, and no offer was made; evidence: `plan/evidence/GOV-011/`.
- GOV-013 completed: `civica-atlas-review-packet/2026-07-v1` binds the frozen G2 archive, codebook, complete schema dictionary, rights/source-input manifests, checksums, release BOM, clean-room instructions/results, coverage/quality reports, limitations, citation, correction policy, and ten bounded questions through 15 byte-hashed link-checked artifacts; no restricted payload was added and no review/contact occurred; evidence: `plan/evidence/GOV-013/`.
- GOV-014 completed: the versioned `governance-evidence-review-packet-2026-07-v2` already binds 49 reproducible tournament/product artifacts and 11 bounded questions; a GOV-specific validator now proves coverage of preregistration, candidates/baselines, panel, code/environment, analyses, sensitivity/uncertainty/subgroups, thresholds, failures, no-winner disposition, misuse, and links while preserving pending-human/no-endorsement status; evidence: `plan/evidence/GOV-014/`.
- GOV-012 prepared but remains open: an owner decision brief recommends equal fixed scope bands of $1,000 for 8–12-hour Atlas/Index reviews and $1,500 for 12–16-hour Pulse reviews, calculates a $12,075 nine-primary ceiling with logistics reserve, defines outcome-independent payment, cancellation, publication, conflict, tax, fee, and institutional-routing safeguards, and queues the exact approval fields; no budget, spending, offer, or contact is authorized yet.
- GOV-001 completed: `civica-research-publication-governance/v1` names Fernando Balino as the current accountable human across all ten required decision domains, records evidence and fail-closed conditions for each, discloses the single-owner concentration, denies agents and anonymous groups decision authority, protects independent reports, and defines conflicts, emergency action, delegation, records, and amendment rules; evidence: `plan/evidence/GOV-001/`.
- GOV-002 completed: `civica-authorship-contributions/v1` names Fernando Balino as the independent responsible author in canonical, Atlas, and Index citation metadata, retains Civica Atlas as publisher, records nine declared roles and three evidenced contribution periods, uses the project contact route, leaves ORCID honestly unavailable, prohibits organization-only concealment, and replaces 15 anonymous blog bylines; evidence: `plan/evidence/GOV-002/`.
- GOV-003 prepared but remains open: the owner-confirmation record supplies publication-ready language and seven exact factual fields for funding, sponsorship, material in-kind support, affiliations/interests, vendor/source relationships, third-party control rights, and update approval; no unsupported no-funding or no-conflict claim was published from repository silence.
- GOV-004 completed: `civica-ai-use-disclosure/v1` publishes eight role-specific model/agent uses on About and in a hashed machine record, names material production systems, separates internal audits and permanently non-gold coding pilots from human review, records incomplete historical metadata honestly, and keeps Fernando Balino responsible for publication; evidence: `plan/evidence/GOV-004/`.
- GOV-005 completed: `civica-release-correction-authority/v1` names Fernando Balino's gated authority, closes major/minor/patch/beta triggers, emergency containment/restoration, historical and DOI relationships, five notice locations, report/appeal routes and conflict handling, and produces complete artifacts for three no-write tabletop incidents; evidence: `plan/evidence/GOV-005/`.
- GOV-006 completed: `civica-advisory-board-charter/v1` publishes the board's purpose, five expertise lanes, advisory-only authority, two-year terms, bounded workload, confidentiality/publicity, conflicts, compensation, departure, consented-publication, and nonendorsement rules; the public roster honestly states that no members have been appointed; evidence: `plan/evidence/GOV-006/`.
- GOV-007 completed: the advisory-board application now matches the five-lane charter, shares client/server validation, requires versioned privacy consent, uses hashed durable rate limiting and a silent honeypot, stores no applicant IP, gives an honest onscreen-only receipt, publishes fields/access/security/18-month retention/deletion/response terms, exposes the same policy on `/privacy#applications`, and gives the protected admin view a retention deadline plus confirmed permanent deletion; live checks left the application table empty; evidence: `plan/evidence/GOV-007/`.
- EXP-043 completed: restored the required `Editorial engraving` label beside the existing `AI-assisted illustration` licensing link after the country-reference header redesign shortened it and broke the production build; the independent editorial-illustration disclosure gate passes; evidence: `plan/evidence/EXP-043/`.
- EXP-039 completed: the `--art` country masthead now defines explicit content and caption grid rows, so the title/stat/tab block anchors at the hero base over the scrim instead of floating mid-hero; geometry verified live at 769/1024/1440 in both themes with the caption keeping its reserved final row, in commit `fix(design): anchor masthead content and guard buttons (EXP-039, EXP-040)`; evidence: `plan/evidence/EXP-039/`.
- EXP-040 completed: editorial prose anchor rules now exclude `.btn`, restoring canonical primary-button colors on `/civica-index` in both themes, with a source-level regression test that fails on any new unguarded prose-scope anchor selector, in the same commit; evidence: `plan/evidence/EXP-040/`.
- EXP-041 completed: `/governance-evidence` now opens on a canonical PageHero landing with hero search and the full sovereign-state index instead of a hardcoded Japan default; unknown slugs land on the selector, deep links keep the country view with its rights links, and the new tokens-only `.editorial-index-grid` primitive is demoed on `/design-system` and documented in DESIGN.md, in commit `feat(design): give governance evidence its flagship landing (EXP-041)`; evidence: `plan/evidence/EXP-041/`.
- EXP-042 completed: every rankings metric column now names its backing publisher beside the provenance dot, a warn-toned flag exposes genuinely mixed columns (median age surfaced as `mixed · mostly CIA World Factbook`), the banner explains the one-source-per-column rule with a link to country-page reconciliation, and the latent unmapped `unesco_uis` label was fixed, in commit `feat(design): name the source on every rankings column (EXP-042)`; evidence: `plan/evidence/EXP-042/`.
- EXP-005/EXP-006 prepared, both remain open pending owner approval: the draft measurable color contract (`plan/decisions/engraving-color-contract-v1.md`), a deterministic non-destructive split-tone grader, metrics, and a self-contained side-by-side pilot sheet for the nine owner-flagged countries now exist under `plan/evidence/EXP-006/`; flagged originals measure 46–80% strong-orange pixels versus ≤0.3% after recipe B, France/UK are recorded as regeneration candidates, and the owner decision is queued in `plan/MANUAL-CHECKS.md`.
- EXP-006 completed: the non-destructive de-orange pilot closed with owner approval — v1's split-tone was rejected as overcorrection and preserved as the fail-direction example, the v2 interactive strength sheet (six exactly pre-rendered steps per country) let the owner calibrate against the intentional sepia identity, and the owner selected strength 60; evidence: `plan/evidence/EXP-006/`.
- EXP-008 completed: all 196 tracked dark engravings were graded in one deterministic batch at the approved strength 60 (saturation ×0.730, hue −6.0°, brightness ×1.018), with `gbr` excluded as a palette-outlier regeneration candidate, originals exactly recoverable from git, a per-file before/after metric report checked in, and live dark-mode browser sampling on the Japan masthead, in commit `feat(art): grade dark engravings at owner-approved strength 60 (EXP-006, EXP-008)`; evidence: `plan/evidence/EXP-008/`.
- EXP-005 completed: the once-draft engraving contract now records the owner's approved Japan light anchor and strength-60 dark grade, exact reference hashes, light and corpus measurement ranges, tone/contrast floors, cool-shadow/gold-highlight limits, line/geometry and landmark invariants, 1500×1000 sRGB WebP q88 delivery, and explicit too-warm/too-cool/style/outlier failures; a machine-readable contract and validator bind the anchors to the 196-asset batch report; evidence: `plan/evidence/EXP-005/`.
- EXP-007 completed: `grade:engravings` now applies the approved versioned grade to one source or a manifest, writes deterministic preview/final WebP outputs plus parameter/hash/geometry/color-metric sidecars, strips metadata without resizing, treats exact reruns as no-writes, refuses a graded output as a new input, and passes a fixed golden-image hash/metric suite plus manifest and double-processing tests; evidence: `plan/evidence/EXP-007/`.
- EXP-010 completed: `civica-editorial-illustration-manifest/v1` inventories all 538 tracked WebP illustrations—394 country, 112 territory, 14 page, and 18 shared assets—with entity/route/subject/caption, exact light-dark pairing, dimensions/bytes/color space/hash, first Git capture, strength-60 edit evidence, rights/disclosure, and QA state; missing original model/tool/prompt/reference sessions are explicitly irrecoverable rather than reconstructed, and regeneration is deterministic with zero missing pairs or captions; evidence: `plan/evidence/EXP-010/`.
- EXP-011 completed: `validate:country-engravings` now gates all 394 country assets/197 pairs for lowercase ISO3 WebP naming, exact light-dark partners, 3:2 approved dimensions, 75–750KB bounds, captions, manifest rows, accepted dark color state, duplicates/missing rows, and zero raw PNG files or runtime fallback; seeded fixtures prove every failure class is caught, and the aggregate editorial-illustration/build gate runs it; evidence: `plan/evidence/EXP-011/`.
- EXP-012 completed: every engraved `PageHero` now carries a visible `Editorial illustration · AI-assisted, non-documentary` policy link while its background remains decorative; country/territory captions keep their concise landmark line and exact visible disclosure with a non-documentary accessible name; `/licensing#imagery` links the complete 538-asset manifest, distinguishes asset coverage from irrecoverable session metadata, names automated and human-review limits, and forbids treating art as source evidence; source-level, claims, desktop/mobile/light/dark, Japan/Greenland/About, and anchor checks pass; evidence: `plan/evidence/EXP-012/`.
- EXP-013 completed: country/territory engraving images and captions now share a valid semantic `<figure>` while `display: contents` preserves the two-row masthead grid; ParallaxImage tracks the enclosing section rather than the semantic wrapper, and exact 769px dark plus 1440px light geometry shows the reserved caption row inside the hero with zero overlap against title/stats or Map/Images controls, zero horizontal overflow, and a clean console; evidence: `plan/evidence/EXP-013/`.
- EXP-037 completed: the country map preview, one full-tile activation button, and two provider-attribution links are now sibling elements; noninteractive MapLibre previews inject no controls, focus returns to the activation after the modal closes, pointer and Enter activation open the map, the live browser reports zero nested controls, and focused accessibility, TypeScript, and design-token checks pass; evidence: `plan/evidence/EXP-037/`.
- PUL-031 completed: stable incident IDs now connect raw reports, event projections, source trails, assignments, and append-only resolution findings; the hash-pinned live repair confirmed five duplicate pairs, retained 474 weaker candidates without merging them, recalculated the 380 current event projections under the incident-aware score version, and left zero invalid, duplicate-current, running-run, or stale-delta states; evidence: `plan/evidence/PUL-031/`.
- PUL-032 completed: `pulse-classification-state/v1` persists one configuration-keyed current state plus an append-only attempt ledger for every cluster, orders never-attempted work before bounded retries, makes terminal-none reruns model-free, and exposes live queue depth and age; production is at migration 25/25 with 384 classified backfills, one terminal failure, and 841 untouched new clusters; evidence: `plan/evidence/PUL-032/`.
- PUL-033 completed: `pulse-review-sla/v1` creates one severity-prioritized obligation per new pending event, persists escalation and bounded exceptions, withholds completeness on breach or unknown state, and retains the 175-item pre-contract queue as unpublished non-human legacy quarantine; production is at migration 26/26 with zero active or breached obligations; evidence: `plan/evidence/PUL-033/`.
- PUL-034 completed: scalar Pulse v1 is terminally retired rather than translated into v2—case-insensitive `sort=cp`, legacy embeds, authenticated cron routes, and CLI entry points fail deterministically without database/model work; production migration 27/27 removed the two verified-empty scalar relations while retaining all 462 legacy event rows, and API/docs/cache/schema/live/browser/build gates pass; evidence: `plan/evidence/PUL-034/`.
- PUL-035 completed: score recomputation now covers current-event and prior-state jurisdictions, excludes future and 366-day-old events, records internal zero tombstones while the public API returns `null`, and writes every projection to append-only output history; production migration 28/28 preserves 650 historical outputs across two runs, current rows have zero stale signals or history mismatches, and 907 tests, live invariants, browser checks, and the production build pass; evidence: `plan/evidence/PUL-035/`.
- PUL-036 completed: classifier agreement and automatic-publication eligibility now derive from retained provider-distinct, prompt-versioned runs; one-run and subscription-agent outputs always queue, 191 unsupported automatic publications were quarantined while 13 human-reviewed publications were preserved, Pulse APIs expose product-specific method metadata, and 912 tests, live invariants, API contracts, browser checks, and the production build pass; evidence: `plan/evidence/PUL-036/`.
- PUL-037 completed: event absorption is now an append-only decision linked to an exact event and sequential comparable fixed-scale Index observations; unsupported, model-only, same-period, changed-scale, and opposite-direction cases fail closed, scoring applies confirmed absorption without mutating corroboration evidence, production is at 29/29 authoritative migrations with zero invented absorption rows, and 920 tests, live invariants, browser checks, and the production build pass; evidence: `plan/evidence/PUL-037/`.
- PUL-038 completed: the exact official RSF 2026 capture now has immutable release metadata and complete observed-or-explicit-missing coverage over all 251 supported non-aggregate jurisdictions; all 384 existing events retain historically unrecoverable pins rather than later values, future inserts pin the adopted release automatically, production weighting/observability remain disabled, and 924 tests, live invariants, production scoring, browser QA, and the full build pass; evidence: `plan/evidence/PUL-038/`.
- ATL-009 completed: cross-corpus constitutional full-text search is applied and verified — a new immutable `constitution_passages` relation (migration `0030`, 96,126 backfilled passages across 186 constitutions) with a stored `tsvector`/GIN index, indexed `websearch_to_tsquery` ranking, jurisdiction/topic filters, escaped highlights, keyset pagination, per-passage provenance, and a rights block (`bulkExport: blocked`); a canonical `/constitution/search` reader page and `/api/constitution/search` + `/api/constitution/passages/[hash]` APIs; distinct ok/no-results/400/outage states. DB execution p95 2.5 ms; 15 focused unit tests, 992-test suite, both search validators, and the full production build pass; browser QA on results and no-results states is clean. Rights (DAT-003), schema dictionary (DAT-009), and retention (DAT-016) contracts plus the GOV-013/GOV-014 review packets and the G2 RC were propagated to reflect the new relation and Constitute rights record. Evidence: `plan/evidence/ATL-009/` (completion.md). Finished from Codex's in-progress working tree.
- PLT-002 completed: lint is now deterministic and enforceable via a ratchet baseline. The eslint flat config already ignores `.next/**` and `.claude/worktrees/**`; a full run is ~18s (declared 120s ceiling). `scripts/lint-baseline.json` freezes the pre-existing 29 errors (22 file/rule groups) and `npm run validate:lint` fails on any new file/rule key or higher count while the backlog only ratchets down. A seeded new `any` violation failed the gate (exit 1) and passed again after removal. Evidence: `plan/evidence/PLT-002/`.
- ATL-025 completed: all 20 pre-master-plan dated plan documents and 36 CIV-208 mockups are reconciled against the atlas-first release in `plan/evidence/ATL-025/reconciliation-ledger.md` — each tagged imported/completed/superseded/deferred with per-item evidence, every promised feature verified present in the tree, residual open work confirmed carried by existing master task IDs, and the acceptance clause verified (no canonical surface points to a noncanonical dated plan as an execution source; the two `.env.example` citations are operator design-references). Documentation-only; evidence: `plan/evidence/ATL-025/`.
- PLT-003 completed: the Turbopack full-project tracing warning is resolved. `MarkdownContent` read prose via `fs.readFile(path.resolve(process.cwd(), file))` over an unbounded path, so the tracer pulled the whole project into every markdown page; the read is now inline-scoped to `content/**` (`path.join(process.cwd(), "content", rel)`) with a new exported `contentRelative()` guard that rejects `../`/absolute/empty paths. Build now emits zero Turbopack warnings (was 1) and 107/107 static pages; `/about` renders intact; 4 path-guard regression tests, typecheck, and lint pass. Evidence: `plan/evidence/PLT-003/`.
- PLT-007 completed: added secret + sensitive-artifact scanning (`scripts/secret-patterns.ts` detectors, `scripts/scan-secrets.ts` with `validate:secrets` tree gate wired into CI and `validate:secrets:history` pre-release scan), with redacted findings, a narrow documented allowlist, and 5 unit tests. Seeded keys and dumps fail; the tree is clean across 2,867 files. The history scan surfaced a REAL past exposure — a live Neon DATABASE_URL committed in `9332c4bc`/`b2bafdd2` (not in the current tree) — recorded by non-reversible hash and queued as a CRITICAL owner rotation in `plan/MANUAL-CHECKS.md` (plaintext never committed). Evidence: `plan/evidence/PLT-007/`.
- PLT-004 completed: dated framework compatibility audit (`plan/evidence/PLT-004/compatibility-report-2026-07-12.md`) against installed Next 16.2.7 / React 19.2.4 / Drizzle 0.45 / Neon HTTP 1.0 / Tailwind v4 and their bundled docs. Verified async params/searchParams (42/16 Promise-typed, 0 sync), cacheComponents-off consistency with `export const dynamic`, no middleware/proxy, top-level Turbopack config, clean server/client boundaries, no unsupported neon-http transactions, no removed React 19 / Next 16 / Tailwind v3 APIs. Zero confirmed mismatches → no remediation task filed; the only latent item is a future `cacheComponents:true` flip. Evidence: `plan/evidence/PLT-004/`.
- PLT-005 completed: adopted a dependency/vulnerability/lockfile policy (`plan/evidence/PLT-005/dependency-policy.md`) — direct-dep license inventory (all permissive), `engines.node>=22`, lockfile-integrity via `npm ci`, weekly Dependabot (`.github/dependabot.yml`, npm + github-actions), an emergency process, and a `validate:deps` gate (`npm audit --audit-level=critical --omit=dev`) wired into CI that blocks critical production findings. Current state: 0 critical (gate passes); the one high (`protobufjs` transitive DoS, fix available, low practical risk) is tracked for the next Dependabot bump. Evidence: `plan/evidence/PLT-005/`.
- PLT-026 completed: fixed a false-404 bug where a DB failure during country identity resolution was caught into `null` and rendered a cacheable/indexable 404 (de-indexing risk). Removed the error-swallowing `.catch(() => null)` from the identity resolver in country layout/page/constitution so DB errors bubble to a new error boundary (500, non-indexable) while genuinely absent slugs still render the branded 404; added `error.tsx` (tokenized, retry, logged) and `global-error.tsx` (own html, noindex). `civica-data` (Index-protected) is backstopped by the shared layout. A 2-test guard scans all app pages and both boundaries; browser confirms a missing slug still 404s; build green. Evidence: `plan/evidence/PLT-026/`.
- PLT-006 completed: added a typed per-context environment contract (`src/lib/env/contract.ts`) declaring required/secret/degrades for build/dev/test/scripts/cron/admin/chat/production, a secret-free `checkEnv()`, and a `validate:env --context=<ctx>` CLI wired into `prebuild` (build) and `predev` (dev) so a missing/invalid required var fails early with a clear message. Optional features degrade with an explicit NOTICE; secrets are never echoed (proven by test). 7 focused tests, full suite 1013/1013, doc-references, typecheck, and lint pass. Evidence: `plan/evidence/PLT-006/`.
- PLT-024 completed: wrote seven operational runbooks (`data/OPERATIONAL-RUNBOOKS.md`) — data-source breakage, bad release, compromised credential, model outage, stale map/assets, legal takedown, incorrect country fact — each with detection/containment/owner/rollback/communication/evidence/recovery referencing the real mechanisms, plus a dated tabletop that records actual gaps (the live unrotated leaked Neon credential, monitoring not yet built, pull-only communication, manual rollback, counsel-gated legal). Evidence: `plan/evidence/PLT-024/`.
- PLT-013 completed: audited and hardened security headers in `next.config.ts` — added HSTS (1yr, includeSubDomains, preload) and a minimal `Content-Security-Policy-Report-Only` allowlisting only the real externally-loaded origins (OpenFreeMap, Vercel Blob PMTiles, Mapbox; fonts self-hosted, no external scripts/image CDNs). Kept nosniff/Referrer/Permissions and the embed-only framing exception (verified live: `/` frame-locked, `/embed/japan` framable). CSP is Report-Only because enforcing script/style-src under Next's App Router needs per-request nonces; enforcement follows a production observation window. 4-test policy guard, atlas map clean of CSP violations, lint pass. Evidence: `plan/evidence/PLT-013/`.
- QA-015 completed: added link/asset/redirect integrity tests (`src/lib/qa/link-asset-integrity.test.ts`) composing with the existing `validate:metadata` (canonical/sitemap/robots) and `doc-sources`/`doc-references` (routes/anchors) guards — the three remaining gaps: required footer links survive (AGENTS invariant: blog/api-docs/design-system/status/licensing/contact/github), every literal local asset reference resolves in `public/` with exact case (catches Linux-404 case mismatches), and no redirect chains (no 308→308). 3/3 pass, part of `npm test`, lint clean. Evidence: `plan/evidence/QA-015/`.
- BRD-011 completed: audited every shipped visual-asset class into a license inventory (`plan/evidence/BRD-011/asset-license-inventory.md`) — engravings/blog (AI illustration, EXP manifest+gate), flags (flagcdn public-domain + emoji fallback), leader portraits (Wikimedia Commons hotlinked with the required credit line surfaced), fonts (OFL via next/font), maps (OSM/ODbL + in-UI attribution), logo/icons (own). No unknown/incompatible files. The audit surfaced that flags (flagcdn.com) and portraits (commons/upload.wikimedia.org) were missing from the PLT-013 CSP `img-src` — added them so the report-only policy (and eventual enforcement) matches reality. Residual: per-file Wikimedia license verification is a data-quality task. Evidence: `plan/evidence/BRD-011/`.
- BRD-009 completed: added a non-commercial-source monetization gate (`src/lib/rights/monetization-gate.ts` + `validate:monetization-gate`, wired into `build`) that derives the restricted-source set from the rights manifest (5: IPU, Constitute, IDEA, GPI, BR) and fails closed when `CIVICA_COMMERCIAL_DEPLOYMENT`/`CIVICA_FEE_BEARING_ACCESS` is on while any is active — blocking paid subscription/API/embed/commercial release until relicensing/removal. Live PASS at the current non-commercial posture; FAILs (exit 1) under a seeded commercial posture; 5 tests. Evidence: `plan/evidence/BRD-009/`.
- BRD-014 completed: published a canonical `/accessibility` page (WCAG 2.2 AA commitment, honest known limitations routed to EXP-020/027/022, feedback via /contact) plus a responsible-disclosure/safe-harbour statement, and `public/.well-known/security.txt` (RFC 9116, served 200), linked from the footer. Renders on the design system with no console errors; metadata/QA-015/typecheck/lint pass. The real-email-delivery + triage dry-run is queued as an owner manual check. Evidence: `plan/evidence/BRD-014/`.
- BRD-015 completed: documented the copyright/trademark/data-rights complaint-handling process (`data/COMPLAINT-HANDLING.md`) across all eight required stages (authenticate, preserve evidence, temporary containment, legal/editorial review, counter/appeal, final action, requester response, public correction + version history), composed from existing mechanisms (contact/dispute/corrections intake, DAT-016 append-only retention, reversible `withheld` value-state + NC flags for containment, counsel escalation, CLM-016 correction/version publication) and preserving complainant privacy. A tabletop note records the two external dependencies (counsel; real intake delivery, queued under BRD-014). Evidence: `plan/evidence/BRD-015/`.
- BRD-008 completed: verified that data rights are published release-specific (per source/field) rather than as one blanket license, composed from shipped work — reader-accessible DAT-003 manifest, the DAT-017 export embedding each fact row's SOURCE_RIGHTS (licenseId/terms/publicExport/commercialUse/derivatives/attributionRequired/restrictions) with named exclusions, and the CLM-013/018 metadata contract binding Dataset.license to the rights registry with a free-access-≠-reuse disclosure. A 3-test guard (`release-rights.test.ts`) asserts no blanket grant, embedded attribution, and registry-bound metadata. Evidence: `plan/evidence/BRD-008/`.
- ATL-017 completed: verified government-taxonomy and peer-lens outputs against the adopted external classifications and current source vintages. Extracted pure, DB-free resolver seams (resolveMaterialPeerSet/Governance/RegimeAlternate; behavior-preserving) so 28 source-backed fixtures exercise every fallback rung, the minimum-n boundary, explicit noncoverage markers, structural/BR-CGV derivation and divergence overrides (Switzerland/Vatican/Andorra), WB region/income + V-Dem RoW + monarchy metadata, pinned BR/CGV vintages (ref year 2022, QoG Jan26, v6.1, taxonomy 2026_v1), and a guard that structural_family cannot re-enter the peer-lens contract. Also repaired 5 pre-existing repo typecheck errors from earlier-session commits (fs.globSync types, an es2018 regex flag, MonetizationEnv weak-type) so `tsc`/`next build` are green. Full suite 1053/1053; Switzerland civica-data renders clean. Evidence: `plan/evidence/ATL-017/`.
- EXP-002 completed: built the executable UI-pattern → canonical-binding map (`src/lib/design/ui-pattern-map.ts`, 14 families / 38 patterns) with a pure validator that verifies every referenced token is defined, every primitive file exists, every class is declared, all 14 families are covered exactly once, and every approved-exception/unmatched pattern names an owning follow-up task (0 unmatched; 2 exceptions → EXP-003 and the owner-gated shadow rename). Added `npm run validate:ui-pattern-map` + 10 tests (real map clean + 6 seeded negative controls). This is the canonical reference for the EXP-028 blind audit. Full suite 1063/1063; tsc clean. Evidence: `plan/evidence/EXP-002/`.
- EXP-004 completed: verified that live reader-style pages compose the shared editorial/methodology primitives with zero page-local <style> blocks (no migration needed — the architecture already achieved it). Locked it with a guard (`reader-page-styling.test.ts`, 3 tests): no inline <style> in any src/app page/layout .tsx, reader-style document pages compose a canonical container + editorial-section/MarkdownContent, and legal/policy pages use methodology-layout + ReaderSidebar. /privacy renders clean in the browser. Persistent visual-regression baselines remain EXP-025. Evidence: `plan/evidence/EXP-004/`.
- EXP-032 completed: restored the country Constitution tab to the canonical three-tab shell. Added an additive showOutline prop to the shared ConstitutionReadingColumn (default true → standalone Explorer unchanged); the tab now renders the exact .factbook-tab > .civica-data-body > .factbook-left-rail grid with CountryJumpSearch + FactbookSidebar (outline items = constitution parts/articles with scroll-spy), the reading body with showOutline={false}, a single masthead H1, and a non-duplicating h2. Verified reading (US, 29 sidebar items) + empty (Greenland) states, mobile collapse, dark tokens, zero overflow, no console errors, and the standalone Explorer unregressed. tsc clean, design-tokens no new drift, +5 guard tests. Evidence: `plan/evidence/EXP-032/`.
- QA-009 completed: established the canonical Playwright/browser harness (`npm run test:e2e`). Config reuses a running dev server and never auto-spawns a second `next dev` (which corrupts the shared Turbopack cache); CI sets `E2E_WEBSERVER_CMD`. The `errors` fixture captures console/page/network/>=400 failures with a `hardFailures()` gate; helpers cover theme, the six-viewport matrix, horizontal-overflow measurement, and env-gated admin login. A 6-test selftest proves the harness drives the real app, captures each seeded failure class, and would fail on a seeded route/asset error. Evidence: `plan/evidence/QA-009/`.
- EXP-019 completed: responsive matrix — every canonical route (30, one per DESIGN.md layout row + all primary reader routes) loaded at the six declared viewports (360/480/768/1024/1280/1536) and asserted to have zero horizontal overflow of the root scroller = 180 checks, all pass, no layout change required (shared editorial shells hold). Durable Playwright spec on the QA-009 harness. First run caught two test-robustness bugs (networkidle hang on the live map/blog) and one non-reproducing design-system hydration artifact (disproved against the SSR HTML). Evidence: `plan/evidence/EXP-019/`.
- QA-004 completed: live-DB tests are read-only and separable. Added getLiveReadOnlyDb() (refuses outside RUN_DB_TESTS=1, mutation methods throw) + reportLiveTestEnvironment() (credential/endpoint-redacted), a DB-free guard test, and a static scanner proving no test writes to the production db (seeded-fixture proven). worked-examples.test.ts now reads via the read-only client. Full DB-free suite 1095/1095; test:db runs read-only. Separate pre-existing finding: 2 worked examples (Brazil GDP growth, SA unemployment) fail on test:db due to live-data drift since DAT-007 — a reconciliation follow-up, not a QA-004 regression. Evidence: `plan/evidence/QA-004/`.
- PLT-008 completed: route-handler security/exposure inventory. Registered all 99 route.ts handlers (registry.ts) by exposure (admin=11, chat=1, cron=39, embed=1, export=3, public-mutation=3, public-read=35, pulse-coding=6), methods, mutation, sensitivity, and declared controls. Pure checks (checks.ts) + validate:route-inventory fail CI on any phantom route, stale entry, or method drift, and flag mutable/sensitive routes without explicit controls; wired into the claims-docs aggregate gate. 17 tests (positive on the real 99 + seeded negatives per failure mode). Two benign uncontrolled-mutation findings flagged and documented (admin & pulse-coding POST sign-out endpoints clear cookies with no prior session check — safe no-ops; candidates for PLT-009/PLT-012 CSRF hardening). Evidence: `plan/evidence/PLT-008/`.
- EXP-036 completed: Record/article metadata & image rendering. Fixed og:image to use the resolved page cover (resolveBlogSocialImage) instead of the usually-null raw frontmatter; removed the nested/duplicate <main> on both the blog index and article (root layout owns the single main); converted inline MDX figures to next/image with REAL intrinsic dimensions (read from the WebP header — engravings aren't all 16:9) for zero layout shift, responsive sizes, and alt="" (figcaption carries the description). Added validate:blog-media (webp + readable dims + 1 MiB source ceiling; 57 files pass) to the build. 9/9 unit + 4/4 e2e; SSR-confirmed. Evidence: `plan/evidence/EXP-036/`.
- QA-007 completed: deterministic golden tests for every published calculation. Added a version-tagged golden-tests registry (civica-golden-tests/v1) mapping all 8 Done-when subtopics → golden file(s) + source module(s) + protected Index transform(s), a DB-free validator (validate:golden-tests, wired into build), and filled the three gaps: Conditions identity passthrough (asserts NO composite), Index candidates B0–B3/K1–K5 exact composites/hashes on a deterministic development-split panel, and Pulse decay/classification exact deltas. Normalization, reconciliation, taxonomy/peer-lenses, uncertainty/sensitivity, and methodology worked-examples were already covered. 70/70 tests, 8/8 subtopics. Evidence: `plan/evidence/QA-007/`. (Built by an Opus subagent; verified + wired in the main session.)
- ATL-019 completed: stable entity identifiers & citations for all 8 kinds (fact, institution, office, person, election, constitution-passage, organization, indicator). New EntityCitation contract (stable-entity-citation/v1) with a closed enum, strict Zod, and per-kind resolvers whose stable id is a UUID primary key (or the already-shipped constitution-passage sha256 digest) — never a mutable display name. /api/citations/[entityType]/[id] dispatches with closed-enum + id-format 404s. Institution/office/person/election source the release/version leg from the DAT-016 evidence-retention ledger; fact/indicator use native source_id/vintage. 24 tests incl. a genuine rename round-trip per kind (id + citationUrl unchanged after a display-name change); static + live validators pass; the new route is registered in the PLT-008 inventory (99→100). Evidence: `plan/evidence/ATL-019/`. (Sonnet subagent; verified + wired in main.)
- EXP-023 completed: standardized alternative-text & decorative-image treatment. New DESIGN.md "Alternative text" policy (flags/portraits/maps/charts/engravings/org-marks/icons = meaningful vs decorative). CountryFlag gained an optional decorative prop (defaults true = alt="" aria-hidden) — 16 non-protected sites explicit; LeaderPortrait, country map/photo gallery, FactbookLightbox, FactValueDot, and 6 more pre-existing decorative images fixed. New baseline-ratchet validate:alt-text-policy gate (fails on any img missing alt or a decorative img missing aria-hidden; 15 seeded-fixture tests) wired into build, plus a 9/9 accessible-names e2e. Index change-control constraint: the 3 protected presentation files (RankingsMatrix, governance-evidence, FactbookHeaderStrip) are left unedited — flags stay decorative via the new default; FactbookHeaderStrip's 2 raw cover-tile imgs are the only baselined deferrals (aria-hidden pending an intentional Index presentation revision). tsc, design-tokens, editorial-illustrations, index-change-control, 1167 tests, and the e2e all pass. Evidence: `plan/evidence/EXP-023/`. (Sonnet subagent; Index-constraint resolution + verification in main.)
- BRD-013 completed: aligned Terms/API/embed/download conditions with real capabilities and rights. New terms-contract.ts clause registry (10 topics) + pure validateTermsConditions + validate:terms-conditions gate (wired into claims-docs). Added Terms #downloads and #embedding sections (free access ≠ reuse license; legacy iframe embed retired/410; per-source rights). Fixed real stale-copy bugs in api-docs (overview/rate-limits/CORS) and a false docstring in rate-limits.ts. Notable: the subagent verified via manifest+registry+git+live curl(200) that the country export route ALREADY works (DAT-027 shipped it with per-fact rights filtering) — corrected my brief's wrong "withheld/503" premise to the current truth. 30/30 clause tests; validate:terms-conditions/api-docs/rights-claims/policy-surface/design-tokens pass; /terms + /api-docs browser-verified. Evidence: `plan/evidence/BRD-013/`. (Sonnet subagent; verified + wired in main.)
- QA-002 completed: module-scoped coverage requirements (not one misleading global number). Added scripts/module-coverage-config.json (8 high-value modules with declared line/branch/function thresholds) + validate:module-coverage (spawns one node --experimental-test-coverage child per module, gated by Node's exit code), enforced in CI via .github/workflows/claims-docs.yml. Behavior-preserving session.ts refactor extracted verifySessionCookie (unit-testable without Next request context; reviewerId still env-derived per PLT-027) + 20 admin-auth tests; outcomesReducer extracted to its own module (CSS-import-free) + tests; markdown substitute tests. Seeded-regression proof (well-covered passes, under-covered fails); the subagent also caught+fixed a real NODE_TEST_CONTEXT-inheritance bug that silently skipped the coverage child. Full suite 1256/1256; 8/8 modules meet thresholds. Evidence: `plan/evidence/QA-002/`. (Sonnet subagent; session-refactor reviewed behavior-preserving + verified in main.)
- ATL-032 completed: government-type/regime trajectories are historically honest. Audit found the public surfaces already satisfy the "current cross-section" branch: the only "long-run trajectory" surface (GovernmentTypesAccordionExplorer) is non-public (its page redirects; nothing imports it), and the live GovernmentTaxonomyBlock renders the regime as a point-in-time cross-section stamped with its reference year, no over-time language. BR/CGV is a single 2022 cross-section reference year (DAT-025), stamped or null — never a fabricated current year. Locked with a 6-fixture source-backed + pure-function test (runs under npm test in the build gate). No production code change needed. Evidence: `plan/evidence/ATL-032/`.
- QA-005 completed: public/admin/cron/API contract & authorization tests. route-authorization.test.ts (12) statically cross-checks every admin/cron/pulse-coding route in the PLT-008 registry actually CALLS its declared guard (requireCronAuth/getAdminSession/getPulseCodingSession) — catching a declared-but-uncalled control the registry scanner didn't. api-contract-shape.test.ts (8) strict-parses every /api/v1/* example against its full response schema. e2e/qa-005-route-authorization.spec.ts (17) proves at runtime that unauthenticated admin/cron/pulse-coding requests reject 401, public-mutation rejects malformed bodies 4xx (never 500), and /api/chat rejects empty/malformed/oversized input BEFORE the Anthropic client is constructed (no paid call). No undocumented auth gap found (only the 2 pre-existing sign-out routes). Full suite 1282/1282; e2e 17/17. Evidence: `plan/evidence/QA-005/`. (Sonnet subagent; verified in main.)
- ATL-012 completed: audited organizations/memberships as dated relationships. Fixed the static curated org path — membership `status`/`endYear` now render as explicit "Withdrawn" badges + join–end year ranges (Burkina Faso/Mali/Niger ECOWAS 2025 exit), current-vs-historical split in stats/map, and a `civica_curated` SourceDot provenance footer (page previously had zero attribution). The DB org path was already honest for source/vintage (real last_sync_at + SourceDot). 10-test fixture locks the represented dimensions (with a "deferred-gap lock" that trips if a future migration adds the missing DB end/status columns). tsc/design-tokens clean, full suite 1292/1292, /organizations/ecowas browser-verified. Evidence: `plan/evidence/ATL-012/`. **Open finding (needs a live data correction, not a migration; forbidden by this read-only task):** the DB's 6 "universal" orgs (UN/WHO/UNESCO/WTO/IMF/IAEA) blanket-stamp all 249 catalog jurisdictions as members — including non-members (Taiwan, Holy See, Antarctica) — the exact "timeless fact" anti-pattern; queued as a follow-up data-repair. (Sonnet subagent; verified in main.)
- ATL-013 completed: audited bills/legislative-activity coverage and narrowed claims to supported jurisdictions. New src/lib/bills/coverage.ts is the single source of truth for the 6 supported jurisdictions (US/GB/CA/DE/FR/BR, from the actual crons) + source/stage/copy. FactbookBills + the public bills API now publish chamber, introduced date, raw status text, shown-vs-total pagination, freshness (SourceDot), and a Banner naming the supported set; the API returns an explicit `coverage` object (naming the 6) for every response instead of a bare empty array. 11 source-backed fixtures; full suite 1303/1303; tsc/design-tokens/index-change-control clean; US/DE bills + Japan + API browser-verified. **Honest deferral:** the in-tab coverage explanation for UNSUPPORTED countries (e.g. Japan) is blocked because the Bills-section visibility gate lives in the Index-change-control-PROTECTED civica-data/page.tsx — unsupported countries currently show silent absence (no misleading empty legislature, the anti-pattern the Done-when targets) rather than an in-context note; the API carries the explanation and the minimal page fix is documented for the next Index-presentation revision. Evidence: `plan/evidence/ATL-013/`. (Sonnet subagent; verified in main.)
- ATL-011 completed: audited party identity/seats/coalition/ideology provenance. Critical (good) finding — no Civica-inferred or unsourced ideology is displayed anywhere: the V-Party v2 sourcing resolution is fully implemented (only match_confidence='high' rows shown; 'review'/fuzzy/non-competitive rows never shown as fact). Real gap found + fixed: PartyExplorer hardcoded every row's seats source as ipu_parline, but ~48% (747/1,548 rows across 16 real chambers) are actually Wikidata-sourced or have no recorded source. Now queries-parties.ts joins the true per-chamber source from statements and PartyExplorer's SeatsSourceCell shows the real SourceDot or an honest "Source not recorded" Chip (browser-confirmed on /parties: 16 not-recorded chips, real per-chamber dots elsewhere). 14 tests (11 pure + 3 live RUN_DB_TESTS), full suite 1317/1317 non-skipped, tsc/design-tokens clean. **Deferred (needs a migration):** the composition writer's delete-then-reinsert gives re-synced parties new UUIDs while party_positions' FK has no cascade — a future reseat of the 157 bodies with matched ideology would hit a silent FK failure; queued as a follow-up schema fix. Evidence: `plan/evidence/ATL-011/`. (Sonnet subagent; verified in main.)
- EXP-018 completed: synchronized desktop/mobile navigation. Audit found 3 of 4 destination lists already unified via shared modules consumed by both NavLinks (desktop) and MobileNav (mobile). Fixed the one genuinely hand-duplicated pair (desktop TRAILING_LINKS vs mobile EDITORIAL_LINKS → new shared editorialNavItems.ts) and the missing mobile group-title current-route active state (extracted desktop's active-state predicates to pure navActiveState.ts, now consumed by both; one globals.css .is-active selector reusing --color-accent). 9-test drift fixture locks zero identity drift (same ids/labels/hrefs/order/external flags from the shared source; external links carry rel="noopener noreferrer"; catches a reintroduced duplicate). tsc/design-tokens clean, full suite 1323/1323, desktop+mobile parity/active-state/external-link browser-verified. Evidence: `plan/evidence/EXP-018/`. (Sonnet subagent; verified in main.)
- EXP-034 (PARTIAL, not yet ticked): document-landmark/heading/label/form-error-linking repairs + a durable validator. Fixed 4 nested-<main> bugs (error.tsx, pulse-coding sign-in/layout, design-system — a second <main> inside the root layout's landmark), a PageHero double-h1 (opt-in titleAs prop; demo renders <p>), a missing design-system h1 (sr-only), form-error association on /contact + both sign-in forms (aria-describedby/role=alert), and accessible names for the Ask Civica chat input + Atlas zoom buttons. New validate:landmarks (baseline-ratchet; scans 227 tsx for nested-main/banner/contentinfo + 64 page.tsx for multiple-h1) wired into build; 19-test guard. tsc/design-tokens/validate:landmarks clean; browser DOM-verified one <main> on 8 routes + live Contact aria-describedby. REMAINING for the tick (Done-when): invalid fields must RECEIVE FOCUS on submit, sticky UI must clear the header, and keyboard fixtures — delegated as a follow-up. (Sonnet subagent; verified in main.)
- EXP-034 completed (follow-up finished the 3 remaining Done-when legs on top of commit 792d66f7): (1) invalid fields now RECEIVE FOCUS on submit — /contact focuses the first invalid control (or the validation summary) on both client-validation and server-422 paths; both server-POST sign-in forms autofocus their role=alert error banner via tabIndex=-1 autoFocus. (2) Sticky UI clears the header — audited every position:sticky site-wide against #site-header (56px, --header-height); fixed one real bug (.compare-section-nav had top:0 + lower z-index → rendered hidden UNDER the header on scroll, 100% overlap) to top:var(--header-height), with .compare-section scroll-margin-top now a token calc; all other sticky surfaces already cleared (measured offsets documented). (3) Keyboard fixture — e2e/exp-034-forms-keyboard.spec.ts (6/6) on the QA-009 harness covers tab-reachability + focus-on-error. tsc/design-tokens/landmarks clean, full suite 1342/1342, new e2e 6/6, qa-005 regression 17/17. Evidence: `plan/evidence/EXP-034/`. (Two Sonnet subagents; verified in main.)
- QA-010 completed: critical reader journeys end to end. Three Playwright specs on the QA-009 harness (qa-010-reader-journeys{,-content,-access}.spec.ts, 23/23) cover all 15 journeys as real interactions — home→search→country tabs, source/provenance, atlas map + selection, two-country compare via the real picker, indicator history, constitution/search, elections + systems, organizations + detail, Record + article, methodology/citation, API docs, download/export, licensing/contact/advisory, 404, and embed. Core journeys run at desktop + small-mobile; dark theme spot-checked on country-tabs + atlas. Read-only, no paid model calls (contact submit never clicked, no /api/chat). Real behavior confirmed: /embed/<slug> is retired (HTTP 410 → /governance-evidence, per api-docs) — the test asserts that 410 contract, not a live widget. No app bug found. Evidence: `plan/evidence/QA-010/`. (Sonnet subagent; verified in main.)
- Plan integrity correction (2026-07-12): ATL-009, ATL-011, ATL-012, and ATL-013 were reopened after their own evidence showed unmet acceptance conditions. The corrected count is 188/303. Existing implementation and audit evidence remains useful, but these tasks stay open until production API latency, stable party identity, complete dated membership semantics and membership repair, and unsupported-country bills disclosure satisfy the written `Done when` clauses.
- ATL-009 completed after reopening: constitutional passage search now closes its production performance and browser gates. The edge-safe API uses one Neon transaction for durable throttling, corpus/filter checks, and indexed search; full passage text is excluded from the result projection. Checked production evidence records DB p95 2.323ms, warm API p95 163.640ms, fresh-process cold p95 710.237ms across 20 restarts, and a 42,965-byte maximum response. Desktop/mobile, light/dark, pagination, source/citation, UK composite, Denmark, Belgium noncoverage, Kosovo status, no-result, and simulated database-outage states passed without browser console errors. Evidence: `plan/evidence/ATL-009/`.
- ATL-011 completed after reopening: party records now use stable `political_parties` identities, immutable source-retrieval runs, soft-retired chamber participation, and append-only identity events. The production migration preserved all 1,548 legislature-party UUIDs and 656 ideology links; live validators found zero broken links or manufactured lineage. The browser ties composition attributes to their actual source/retrieval/license, keeps incomplete legacy provenance explicit, and shows ideology only for complete high-confidence V-Party matches. Desktop/mobile browser checks, live migration and retention checks, 1,350 tests, and the production build passed. The 1,548 migrated identities remain honestly marked provisional until a publisher supplies a stable external identifier. Evidence: `plan/evidence/ATL-011/`.
- ATL-012 completed after reopening: `organization-membership-release/2026-07-v1` replaces public blanket membership claims with 23 source-backed organization identities and 445 database relationships—442 current and 3 withdrawn—while retaining 1,435 former seed rows as nonpublic `unverified_legacy` audit evidence. Migration `0032_sparkling_genesis` is live and fingerprint-verified; the atomic writer converges to zero writes on repeat; current, withdrawn, observer, disputed, interval, roster-scope, source, vintage, and rights fields now flow through country, organization, Compare, Atlas, and API readers. The release distinguishes 9 complete rosters from 14 checked subsets, keeps the mixed-source roster outside bulk export pending rights clearance, refreshes the G2 rights archive and Atlas review packet, and passes live invariants, browser/API checks, 1,360 tests, and the isolated production build. Evidence: `plan/evidence/ATL-012/`.
- ATL-013 completed after reopening: a successful zero-row Bills lookup now remains visible and distinguishes unsupported jurisdiction coverage, supported-but-empty source/sync state, and a lookup outage; Japan shows the shared six-jurisdiction coverage explanation while populated United States behavior is unchanged. Focused/static gates, isolated 1,361-test and claims suites, the direct 108-page production build, and desktop/mobile light/dark browser checks passed. The aggregate build separately exposed mutable-live drift against the frozen Pulse evaluation packet and added PUL-042 without rewriting the freeze. Evidence: `plan/evidence/ATL-013/`.
- BRD-004 completed: adopted a versioned nine-criterion keep/rename rubric covering legal/confusion risk, domains and handles, pronunciation/searchability, mission fit, international meaning, distinctiveness, transition cost, evidence quality, and capped owner preference. Missing evidence or legal clearance records no decision, and personal dislike cannot trigger a rename. The contract records no conclusion about the current name. Focused tests, seeded failure fixtures, the rendered-policy drift check, ESLint, TypeScript, and plan integrity passed. Evidence: `plan/evidence/BRD-004/`.
- PUL-042 completed: retained the exact rights-safe inputs behind the immutable 920-packet evaluation release so the normal build validator is database-free and cannot drift as production advances. Seeded late-row, frozen-input, manifest, and forbidden-payload changes fail closed; `--write` verifies without rewriting. A separate read-only audit found the same 384 event and 978 negative identities plus 43 later `pending`→`non_governance` transitions. The private disabled coding workspace has a separate packet-set hash mismatch registered as PUL-043; no study was mutated. Evidence: `plan/evidence/PUL-042/`.
