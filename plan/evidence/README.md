# Master-plan evidence

Create one directory per completed task: `plan/evidence/<TASK-ID>/`.

Each directory must contain a short `README.md` with the task, commit, commands and exit statuses, data vintages or fixtures, produced artifacts/screenshots, limitations, and any queued manual check. Store only redistributable evidence; use hashes and access instructions for restricted inputs.
