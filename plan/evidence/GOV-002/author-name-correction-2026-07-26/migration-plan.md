# Migration plan — author-name diacritic correction

No database, schema, data, or methodology migration is required.

Mutable sources use `Fernando Baliño`; frozen artifacts keep their original
bytes. Rollback is a source revert and must not be described as changing the
owner's actual name or restoring preferred metadata.
