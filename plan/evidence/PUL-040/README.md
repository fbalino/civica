# PUL-040 — Prospective shadow-period start evidence

Status: start computation and preflight automation complete; the clock has not
started.

`start-readiness.json` is generated by a read-only aggregate audit. It binds the
preregistered protocol hash, frozen `pulse-v2.15-beta` runtime contract, all
five scheduled/review routes, current-method stage completions, live evidence
cut, eight start prerequisites, and the exact non-backdating rule.

The 2026-07-23 audit reports:

- protocol and code/runtime versions are frozen;
- all required routes exist in the repository, but deployed enabled state is
  not verified;
- only `corroborate` and `score` have completed `pulse-v2.15-beta` runs;
- `ingest`, `cluster`, and `classify` have no completed v2.15 run;
- live append-only/output-history validation remains blocked by the pending
  PUL-024/PUL-027/PUL-043 migration/reconciliation work;
- the start-boundary source-coverage/observability snapshot does not exist; and
- no prospective label/window/start record exists.

Therefore `earliestCompliantStart` and `plannedEnd` are null. After every
prerequisite passes, the start is recorded before and equals the first retained
eligible retrieval; the end is exactly 90 consecutive UTC days later. The
window cannot be backdated to the protocol lock or any earlier partial run.

No model call, deployment, migration, label opening, paid activity, or clock
start occurred.
