# EXP-013 migration and rollback note

No data, route, or asset migration is required. This is a semantic markup and shared-layout correction.

Deploy normally. If a browser-specific `display: contents` accessibility regression is discovered, replace the wrapper with another valid figure composition that preserves the separate caption row and re-run the exact-width geometry matrix. Do not revert to a figcaption outside figure or to an absolutely positioned caption. Any rollback must append a new presentation change-control entry.
