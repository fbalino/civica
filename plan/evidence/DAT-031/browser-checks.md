# DAT-031 browser and HTTP checks

- Opened `http://localhost:3000/api-docs` in the in-app browser.
- Confirmed the country detail contract renders `as_of` as required and describes `live` or a complete immutable Civica Atlas vintage label.
- No browser console errors were present.
- Exercised list, detail, and export routes against the local Next.js server with live and frozen selections; the status and metadata results are recorded in `live-selection-audit.json`.
