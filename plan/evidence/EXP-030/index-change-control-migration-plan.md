# Index presentation migration plan — retired embed document v43

No database, source-input, score, rank, or cache-data migration is performed.
The deployed change is limited to the terminal 410 representation returned to
legacy iframe URLs. Existing URLs continue to receive no-store retirement
documents and link to Governance Evidence rather than a mutable scalar output.
