# Pulse source-coverage shared-contract v4 release note

**Contract version:** `civica-index-api-contract-pulse-source-coverage-v4`

The endpoint and response shape remain unchanged. This record corrects the source-coverage golden fixture so it verifies both the jurisdiction count and the ISO3 list returned by the runtime evidence aggregate.

No reader-facing migration notice is required.
