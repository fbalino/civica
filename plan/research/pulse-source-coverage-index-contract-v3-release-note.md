# Pulse source-coverage shared-contract v3 release note

**Contract version:** `civica-index-api-contract-pulse-source-coverage-v3`

The endpoint and response shape remain unchanged. This version registers the methodology's live retained-row and jurisdiction-scope statements in the public numeric-claims registry. It also restores the generated Civica Index normalization table to its canonical form after documentation formatting.

The added tests bind the displayed counts to runtime source-coverage evidence. No historical migration notice is added to the reader site.
