import assert from"node:assert/strict";import{readFileSync}from"node:fs";import{buildIndexOosValidation}from"./generate-index-oos-validation";async function main(){const stored=JSON.parse(readFileSync("data/releases/index-oos-validation-v1/result.v1.json","utf8"));const r=await buildIndexOosValidation();assert.deepEqual(r,stored);assert.equal(r.artifacts.K1.status,"fails_original_information");assert.equal(r.artifacts.K2.status,"fails_stability");assert.equal(r.artifacts.K2.interval.iterationsRequested,2000);assert.ok(r.geographicFinalHoldout.byRegion.every(x=>!x.performanceEstimateAllowed));assert.ok(r.geographicFinalHoldout.byRegime.every(x=>!x.performanceEstimateAllowed));assert.equal(r.winnerSelected,false);console.log(`PASS — OOS audit ${r.resultSha256}: K1 originality and K2 stability fail; undersized geographic subgroups are suppressed.`)}main().catch(e=>{console.error(e);process.exit(1)});
