import assert from"node:assert/strict";import{readFileSync}from"node:fs";import{buildIncrementalInformationAnalysis}from"./generate-index-incremental-information";import{incrementalPreregistrationErrors}from"../src/lib/ci/incremental-information-preregistration";
async function main(){assert.equal(incrementalPreregistrationErrors().length,0);const stored=JSON.parse(readFileSync("data/releases/index-incremental-information-v1/result.v1.json","utf8"));const result=await buildIncrementalInformationAnalysis();assert.deepEqual(result,stored);assert.deepEqual(result.models.map(m=>m.id),["B1","B2","B3","P1"]);for(const m of result.models){assert.equal(m.intervals.iterations,2000);assert.ok(m.intervals.r2.lower95<=m.metrics.r2&&m.metrics.r2<=m.intervals.r2.upper95)}assert.equal(result.originality.passes,false);assert.ok(result.originality.publicInputModelR2>=result.originality.threshold);assert.equal(result.decisionUtility,"not_tested_requires_preregistered_human_tasks");assert.equal(result.noGovernanceOutcomePredicted,true);console.log(`PASS — public inputs reproduce K1 at final-holdout R2 ${result.originality.publicInputModelR2.toFixed(6)}, failing the frozen originality gate.`)}main().catch(e=>{console.error(e);process.exit(1)});
