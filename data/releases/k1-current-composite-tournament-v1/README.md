# K1 current composite tournament candidate v1

K1 is an isolated research implementation of the current four-dimension composite. It uses the exact source-plus-indicator identities and V-Dem-first WGI Voice fallback, fixed normalization, weights 0.27/0.26/0.23/0.24, `ci-missingness/v1`, deterministic rounding, no unsupported score interval, and competition ranking on the published integer score.

The checked manifest contains aggregate coverage and an output hash only. Private country-year values reproduce from panel v3. The live validator also applies the same K1 engine to all 745 current Beta-R5 dimension rows and requires every released score, completeness flag, missing-dimension set, and rank to match.
