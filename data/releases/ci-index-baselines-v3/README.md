# Civica Index tournament baselines v3

V3 reads panel v3 and treats source plus indicator as the input identity. Democratic Quality uses V-Dem LDI when present and WGI Voice & Accountability only as fallback. WGI Rule of Law remains a separate feature. No publisher-level key can overwrite or conflate the two WGI indicators.

B0 preserves the full unit grid and all five governance source-indicator cells. B1 remains V-Dem alone. B2 and B3 select the same four dimensions and fallback precedence available to K1; B2 is equal-weight and B3 fits on development rows only. Values remain private; the manifest contains counts and hashes.
